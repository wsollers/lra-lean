# AlgebraicStructures Repair Status

- Generated: 2026-09-01
- Repo: `F:/repos/lra-lean`
- Branch: `main`
- Source: conversation — AlgebraicStructures reorg + axiom-purity audit
- Workflow: `docs/algebraic-structures-repair/README.md` — one subject at a time,
  strict tier order, two passes (reorg, audit), gated on `validate_structure.py` +
  `lake build`
- Standing decisions: `docs/algebraic-structures-repair/DECISIONS.md`

| Item | Tier | Subject | Status | Depends on | Notes |
|---|---|---|---|---|---|
| as-01 | 1 | Magma | **done** | — | Corrected: MagmaLaws = Nonempty M (not empty). Constructions/{Set,Mathlib} + real Examples (RPS, Z-sub, R\{0}-div, ZFCSet-union). New `LRA_Set` at `LRA/Set/Interop/Switch.lean`. IdentityNotRequired theorem left `sorry` (theorems are the user's to prove); other example proofs stay proved. |
| as-02 | 1 | Semigroup | **done** | — | Same Nonempty gap as Magma, fixed. Found+fixed a duplicate bridge instance in shared MathlibBridge.lean. Examples: pos-ints-under-+, even-ints-under-×, nonempty-lists-under-++. IdentityNotRequired theorem left `sorry`; other example proofs stay proved. |
| as-03 | 1 | CommutativeSemigroup | **done** | — | Added missing `CommutativeSemigroupLaws` combining class (associativity+commutativity+nonempty); `MultiplicativeCommutativeLaws` kept atomic/unchanged since it's shared downstream. Constructions/{Set,Mathlib} + 4 Examples (pos-ints-under-+, Set union/intersection semilattices, pos-even-nats-under-×, min/max on ℝ). |
| as-04 | 1 | AdditiveSemigroup | **done** | — | Same nonemptiness gap fixed. Found a real cross-tier bug (stored-field collision with `MultiplicativeSemigroupLaws` broke every combining class using both sides), fixed same-day by D7: nonemptiness is now `[Nonempty R]`, an ambient parameter, everywhere in the tree (as-01–04 revisited, 22 downstream files updated). Examples: pos-nats-under-+, even-ints-under-+, pos-even-nats-under-+. |
| as-05 | 1 | AdditiveCommutativeSemigroup | **done** | — | Same missing-combining-class gap as as-03: added `AdditiveCommutativeSemigroupLaws` (`[Nonempty R]` per D7). Examples: pos-nats-under-+ (reused), pos-even-nats-under-+ (reused), open-interval-(0,∞)-under-+ on ℝ. |
| as-06 | 1 | Archimedean | **done** | — | No Definitions/ folder needed (property, not new signature). Real Mathlib bridge proved (reindexing + strict adaptation). Examples: ℤ, ℚ (free via bridge). Non-Archimedean Failures/ example deferred — flagged for follow-up. |
| as-07 | 1 | DiscreteInteger | **done** | — | Six independent classes kept per D6 (no combining XLaws). Audit found and fixed a real bug: DiscretenessLaw's acyclicity axiom was only checked at the basepoint 0, not universally as the user's spec states — same D2 "silently wrong axiom" risk as Magma's as-01 correction. Example: ℤ (the essentially-unique model per the spec's own categoricity remarks). No Constructions/ built — no generic Mathlib typeclass matches this subject's classes 1-1. |
| as-08 | 1 | JoinSemilattice | **done** | — | D6 resolved: explicit-operation-argument style is deliberate (Lattice needs join+meet coexisting on one carrier, incompatible with [Mul R] style) — kept, not converted. Examples: Set union, ℝ max (via Mathlib SemilatticeSup bridge). Constructions/OrderTheoretic/InducedOrder.lean added: the algebraic definition induces a genuine order-theoretic join-semilattice (x≤y := x∨y=y), the equivalence-of-definitions theorem the user asked for. All proofs sorry'd per D8, including a missed one (`unionIsJoinSemilattice`) caught and fixed 2026-08-26 during the as-09 session. |
| as-09 | 1 | MeetSemilattice | **done** | — | Exact dual of as-08: explicit-operation-argument style, same D6 reasoning. Examples: Set intersection, ℝ min (via Mathlib SemilatticeInf bridge), ℕ under gcd (the user's third standard example). Constructions/OrderTheoretic/InducedOrder.lean built from the start (x≤y := x∧y=x). All proofs sorry'd from the start per D8. |
| as-10 | 2 | Monoid | **done** | Semigroup | Audit found the existing `MonoidLaws` (combining `MultiplicativeSemigroupLaws`+`MultiplicativeIdentityLaws`) already matched the user's `(M,*,e)` spec exactly — no fix needed. Built all 5 of the user's standard examples plus this process's first-ever `Failures/` folder (positive integers under addition, reusing Semigroup's own example per D3's plan). |
| as-11 | 2 | AdditiveMonoid | **done** | AdditiveSemigroup | Exact dual of as-10/Monoid, audit confirmed correct as-is. Examples: `(ℕ,+,0)` (doubles as the free-additive-monoid-on-{1} example), `NNReal`, `ENNReal`, square matrices under addition (simpler than Monoid's multiplicative version — no Fintype/DecidableEq needed). Failures/ dual built too (positive naturals, no identity). First item this whole process to build clean on the first `lake build` attempt. |
| as-12 | 2 | Lattice | **done** | JoinSemilattice, MeetSemilattice | Audited correct as-is (JoinSemilatticeLaws + MeetSemilatticeLaws + MutualAbsorptionLaw = exactly the user's 4 identities). Important finding: the algebra↔order equivalence already exists pre-built at `LRA/Order/Interop/AlgebraicLattice.lean` (both directions, more complete than either semilattice's own bridge) — did not duplicate it inside `AlgebraicStructures/Lattice`, just fixed its import path. |
| as-13 | 3 | Group | **done** | Monoid | Audit confirmed `GroupLaws` matches the user's 4 axioms exactly (no bug). Caught and fixed a real bug in my own new `ClosedSubsetGroupLaws` builder — it wrongly required `[GroupLaws A]` on the ambient, which fails for the most natural example (nonzero reals under ×, where ℝ is only a monoid). Examples: ℤ, ℝ\{0} (via the fixed builder), Sym(S), GL_n(ℝ). |
| as-14 | 3 | AdditiveGroup | **done** | AdditiveMonoid | Exact dual of Group (as-13), audited correct as-is. Applied Group's own ambient-hypothesis lesson from the start this time (closed-subset builder needs only `[AdditiveMonoidLaws A]`, not `[AdditiveGroupLaws A]`) — no build failure needed to catch it. All 4 examples free via Mathlib bridges, no wrapper structs needed (unlike Group's own item). |
| as-15 | 3 | CommutativeMonoid | **done** | CommutativeSemigroup, Monoid | Audited correct as-is. Examples: `(ℕ,+,0)` (reuses Monoid's carrier), `(ℕ,×,1)`, power set under union, Booleans under conjunction — the last a genuine find, free via Mathlib's existing `BooleanRing Bool` instance rather than built from scratch. Clean on the first `lake build` attempt. |
| as-16 | 3 | BoundedLattice | **done** | Lattice | Checked `LRA/Order/` per user request — unlike `Lattice`'s own equivalence, no order-theoretic `BoundedLattice` or algebra↔order bridge exists anywhere yet; flagged as an explicit open item rather than built (no equivalence-of-definitions section was given this time). Audited: only half of each bound's conditions is stated as an axiom, verified by hand that the other halves are provable consequences (not a gap). Examples: power set, divisor lattice of `n` (fresh construction), extended reals. |
| as-17 | 3 | DistributiveLattice | **done** | Lattice | Checked `LRA/Order/` again — unlike `BoundedLattice`, the order-theoretic definition (`LRA.Order.Lattices.DistributiveLattice`) already exists, but (like `BoundedLattice`) no algebra↔order bridge does; flagged as an open item. Audited: states both distributive identities as separate conjuncts even though the user's spec notes they're equivalent — confirmed deliberate non-minimal redundancy, not a bug. Examples: power sets, divisor lattice, chains — down-set lattices skipped for scope (needs a from-scratch poset construction). Clean on the first `lake build` attempt. |
| as-18 | 4 | Semiring | **done** | AdditiveCommutativeSemigroup, AdditiveMonoid, Monoid | Audit confirmed the existing law bundle is correct as-is: additive commutative monoid + multiplicative monoid + zero-absorbing + distributive, with no shim layer needed despite the pre-existing `BooleanRingOperationBundle` name on the concept-signature side. Added subject-local `Constructions/Mathlib/Laws.lean` (`[Semiring R] [Nonempty R] -> SemiringLaws R`, left `sorry` per D8), removed the shared `SemiringLaws` combining instance from `MathlibBridge.lean`, and added direct examples for `Nat`, `NNReal`, and `ENNReal`. `validate_structure.py --path LRA/AlgebraicStructures/Semiring`, `lake build LRA.AlgebraicStructures.Semiring`, and `lake build LRA.VolumeVIExamplesFailures` all pass on 2026-08-31. |
| as-19 | 4 | AbelianGroup | **done** | AdditiveCommutativeSemigroup, AdditiveGroup | Audit confirmed the existing law bundle is correct as-is: additive semigroup + additive identity + additive inverse + additive commutativity, exactly matching the intended abelian-group contract. Added subject-local `Constructions/Mathlib/Laws.lean` (`[AddCommGroup R] [Nonempty R] -> AbelianGroupLaws R`, left `sorry` per D8) plus direct examples for `Int`, `Rat`, `Fin n -> Real`, and `ZMod n`, with no compat wrapper or shared-bridge fallback introduced. `validate_structure.py --path LRA/AlgebraicStructures/AbelianGroup`, `lake build LRA.AlgebraicStructures.AbelianGroup`, and `lake build LRA.VolumeVIExamplesFailures` all pass on 2026-08-31. |
| as-20 | 4 | OrderedGroup | **done** | Group | Audit confirmed the existing law bundle is correct as-is: `OrderedGroupLaws` remains exactly `GroupLaws + PartialOrderLaws + MultiplicativeOrderCompatibilityLaws`, matching the intended ordered-group contract in this tree. Added subject-local `Constructions/Mathlib/Laws.lean` (`[CommGroup R] [PartialOrder R] [IsOrderedMonoid R] [Nonempty R] -> OrderedGroupLaws R`, left `sorry` per D8), added direct examples for `Multiplicative Int`, `Multiplicative Rat`, and `Multiplicative Real`, and deleted the remaining empty OrderedGroup stub files rather than keeping duplicate empty surfaces. `validate_structure.py --path LRA/AlgebraicStructures/OrderedGroup`, `lake build LRA.AlgebraicStructures.OrderedGroup`, `lake build LRA.AlgebraicStructures.LinearlyOrderedGroup`, and `lake build LRA.VolumeVICore` all pass on 2026-08-31. |
| as-21 | 4 | BooleanAlgebra | **done** | BoundedLattice, DistributiveLattice | Audit confirmed the existing law bundle is correct as-is: `BooleanAlgebraLaws` remains `BoundedLatticeLaws + DistributiveLatticeLaws +` the two complement equations, with only harmless lattice-content redundancy through the reused bounded/distributive bundles. Reorganized the subject to `Interface/{Definitions,Laws}`, added subject-local `Constructions/Mathlib/Laws.lean` with `ofBooleanAlgebra` as a plain theorem bridge from Mathlib's `BooleanAlgebra`, added direct examples for `Set α`, `Prop`, and `Bool`, and deleted the remaining empty stub files rather than keeping duplicate empty surfaces. `validate_structure.py --path LRA/AlgebraicStructures/BooleanAlgebra`, `lake build LRA.AlgebraicStructures.BooleanAlgebra`, and `lake build LRA.VolumeVICore` all pass on 2026-08-31. |
| as-22 | 5 | CommutativeSemiring | **done** | CommutativeSemigroup, Semiring | Audit confirmed the existing law bundle is correct as-is: `CommutativeSemiringLaws` remains additive commutative monoid + multiplicative commutative monoid + zero-absorbing + distributive, matching the intended commutative-semiring contract in this tree. Reorganized the subject to `Interface/{Definitions,Laws}` owners while preserving the existing promoted `Interface/ModelTheory` surface, added subject-local `Constructions/Mathlib/Laws.lean` with `[CommSemiring R] [Nonempty R] -> CommutativeSemiringLaws R` left `sorry` per D8, added direct examples for `Nat`, `NNReal`, and `ENNReal`, and deleted the remaining empty stub files rather than leaving duplicate owner surfaces behind. `validate_structure.py --path LRA/AlgebraicStructures/CommutativeSemiring`, `lake build LRA.AlgebraicStructures.CommutativeSemiring`, `lake build LRA.VolumeVICore`, and `lake build LRA.VolumeVIExamplesFailures` all pass on 2026-08-31. |
| as-23 | 5 | CommutativeSemiringWithoutZero | **done** | AdditiveSemigroup, AdditiveCommutativeSemigroup, Semigroup, Monoid, CommutativeSemigroup, Semiring | Audit confirmed the existing law bundle is correct as-is: `CommutativeSemiringWithoutZeroLaws` remains additive commutative semigroup + additive cancellation + multiplicative commutative monoid + multiplicative cancellation + distributive, matching the intended no-zero contract in this tree. Reorganized the subject to `Interface/{Definitions,Laws}` owners while preserving the existing `Interface/ModelTheory` surface, retargeted direct `NaturalNumbers` and `PositiveNaturals` consumers to the interface owners, added a direct `PNat` example, and deleted the legacy duplicate owner files rather than leaving compat layers behind. No subject-local Mathlib bridge was added because this pass did not find a single exact Mathlib counterpart matching the without-zero contract without semantic weakening. `validate_structure.py --path LRA/AlgebraicStructures/CommutativeSemiringWithoutZero`, `lake build LRA.AlgebraicStructures.CommutativeSemiringWithoutZero`, `lake build LRA.NumberSystems.NaturalNumbers`, `lake build LRA.NumberSystems.PositiveNaturals`, `lake build LRA.VolumeVICore`, and `lake build LRA.VolumeVIExamplesFailures` all pass on 2026-08-31. |
| as-24 | 5 | Ring | **done** | AbelianGroup, Semiring | Audit confirmed the existing law bundle is correct as-is: `RingLaws` remains abelian group + multiplicative monoid + zero-absorbing + distributive, matching the intended ring contract in this tree. Reorganized the subject to `Interface/{Definitions,Laws}` owners while preserving the existing `Interface/ModelTheory` surface, retargeted direct `CommutativeRing`, `NontrivialRing`, `OrderedRing`, `DiscreteInteger`, `VolumeVICore`, and `VolumeVIExamplesFailures` consumers to those owners, added subject-local `Constructions/Mathlib/Laws.lean` with `[Ring R] [Nonempty R] -> RingLaws R` left `sorry` per D8, added direct examples for `Int`, `Rat`, and `Matrix (Fin n) (Fin n) Real`, removed the stale shared `RingLaws` bridge from `MathlibBridge.lean`, and deleted the legacy duplicate owner/stub files rather than leaving compat surfaces behind. `validate_structure.py --path LRA/AlgebraicStructures/Ring`, `lake build LRA.AlgebraicStructures.Ring`, `lake build LRA.AlgebraicStructures.CommutativeRing`, `lake build LRA.AlgebraicStructures.OrderedRing`, `lake build LRA.AlgebraicStructures.NontrivialRing`, `lake build LRA.VolumeVICore`, and `lake build LRA.VolumeVIExamplesFailures` all pass on 2026-09-01. |
| as-25 | 5 | OrderedSemiring | **done** | Semiring | Audit confirmed the existing law bundle is correct as-is: `OrderedSemiringLaws` remains `SemiringLaws + LinearOrderLaws + AdditionRespectsOrderLaws + MultiplicationRespectsOrderLaws`, matching this tree's intended total-order ordered-semiring contract. Reorganized the subject to `Interface/{Definitions,Laws}` owners while preserving the existing promoted `Interface/ModelTheory` surface, retargeted direct `WholeNumbers`, `ZeroBasedNaturals`, `VolumeVICore`, and `VolumeVIExamplesFailures` consumers, added subject-local `Constructions/Mathlib/Laws.lean` with `[Semiring R] [LinearOrder R] [IsOrderedRing R] [Nonempty R] -> OrderedSemiringLaws R` left `sorry` per D8, added direct examples for `Nat`, `NNReal`, and `ENNReal`, and deleted the legacy duplicate owner/stub files rather than leaving compat surfaces behind. `validate_structure.py --path LRA/AlgebraicStructures/OrderedSemiring`, `lake build LRA.AlgebraicStructures.OrderedSemiring`, `lake build LRA.NumberSystems.WholeNumbers`, `lake build LRA.NumberSystems.ZeroBasedNaturals`, `lake build LRA.VolumeVICore`, and `lake build LRA.VolumeVIExamplesFailures` all pass on 2026-09-01. |
| as-26 | 5 | LinearlyOrderedGroup | **done** | OrderedGroup | Audit confirmed the existing law bundle is correct as-is: `LinearlyOrderedGroupLaws` remains `GroupLaws + LinearOrderLaws + MultiplicativeOrderCompatibilityLaws`, matching this tree's intended total-order ordered-group contract. Reorganized to `Interface/{Definitions,Laws}`, added subject-local `Constructions/Mathlib/Laws.lean` with `[CommGroup R] [LinearOrder R] [IsOrderedMonoid R] [Nonempty R] -> LinearlyOrderedGroupLaws R` left `sorry` per D8, added direct examples for `Multiplicative Int`, `Multiplicative Rat`, and `Multiplicative Real`, and deleted the legacy duplicate owner/stub files rather than leaving compat surfaces behind. `validate_structure.py --path LRA/AlgebraicStructures/LinearlyOrderedGroup`, `lake build LRA.AlgebraicStructures.LinearlyOrderedGroup`, `lake build LRA.VolumeVICore`, and `lake build LRA.VolumeVIExamplesFailures` all pass on 2026-08-31. |
| as-27 | 6 | CommutativeRing | **done** | CommutativeSemigroup, Ring | Audit confirmed the existing law bundle is correct as-is: `CommutativeRingLaws` remains ring + multiplicative commutativity, matching the intended commutative-ring contract. Reorganized to `Interface/{Definitions,Laws,ModelTheory}`, added subject-local `Constructions/Mathlib/Laws.lean` with `[CommRing R] [Nonempty R] -> CommutativeRingLaws R` left `sorry` per D8, added direct examples for `Int`, `Rat`, and `Real`, removed the stale shared `CommutativeRingLaws` bridge from `MathlibBridge.lean`, and deleted the legacy duplicate owner/stub files rather than leaving compat surfaces behind. `validate_structure.py --path LRA/AlgebraicStructures/CommutativeRing`, `lake build LRA.AlgebraicStructures.CommutativeRing`, `lake build LRA.VolumeVICore`, and `lake build LRA.VolumeVIExamplesFailures` all pass on 2026-09-01. |
| as-28 | 6 | NontrivialRing | **done** | Ring | Audit confirmed the existing law bundle is correct as-is: `NontrivialRingLaws` remains `RingLaws + NontrivialityLaw`, matching the intended nontrivial-ring contract in this tree. Reorganized to `Interface/{Definitions,Laws}`, added subject-local `Constructions/Mathlib/Laws.lean` with `[Ring R] [NeZero (1 : R)] [Nonempty R] -> NontrivialRingLaws R` left `sorry` per D8, added direct examples for `Int`, `Rat`, and `Matrix (Fin (n+1)) (Fin (n+1)) Real`, retargeted direct `DivisionRing` and `IntegralDomain` consumers to the new interface owner, removed the stale shared `NontrivialityLaw` bridge from `MathlibBridge.lean`, and deleted the legacy duplicate owner/stub files rather than leaving compat surfaces behind. `python scripts/validate_structure.py --path LRA/AlgebraicStructures/NontrivialRing`, `lake build LRA.AlgebraicStructures.NontrivialRing`, `lake build LRA.VolumeVICore`, and `lake build LRA.VolumeVIExamplesFailures` all pass on 2026-09-01. |
| as-29 | 6 | OrderedRing | **done** | Ring | Audit confirmed `OrderedRingLaws` stays `RingLaws + PartialOrderLaws + AdditionRespectsOrderLaws + MultiplicationRespectsOrderLaws`. Reorganized to `Interface/{Definitions,Laws,ModelTheory}`, added subject-local Mathlib bridge plus `Int`/`Rat`/`Real` examples, retargeted minimal direct consumers, and deleted legacy duplicate owner/stub files. |
| as-30 | 7 | DivisionRing | **done** | NontrivialRing | Audit confirmed `DivisionRingLaws` stays `RingLaws + NontrivialityLaw + MultiplicativeInverseLaws`, with `DivisionCompatibilityLaw` kept as a separate atomic law rather than folded into the combined class. Reorganized to `Interface/{Definitions,Laws,ModelTheory}`, moved the concept-signature owner into `DivisionRing` itself instead of depending upward on `Field`, added a subject-local Mathlib bridge plus `Rat`/`Real`/`ℍ[Real]` examples, retargeted minimal `Field` consumers, and deleted legacy duplicate owner/stub files. |
| as-31 | 7 | IntegralDomain | **done** | CommutativeRing, NontrivialRing | Audit confirmed the existing law bundle is correct as-is: `IntegralDomainLaws` remains `CommutativeRingLaws + NontrivialityLaw + NoZeroDivisorsLaw`, matching the intended integral-domain contract without adding axioms or changing the established architecture. Reorganized to `Interface/{Definitions,Laws,ModelTheory}`, added subject-local `Constructions/Mathlib/Laws.lean` with `[CommRing R] [IsDomain R] [Nonempty R] -> IntegralDomainLaws R` left `sorry` per D8, added direct examples for `Int`, `Rat`, and `Real`, retargeted minimal direct consumers in `GaussianIntegers`, `Integers.Interface.ModelTheory`, `VolumeVICore`, and `VolumeVIExamplesFailures`, removed the stale shared `IntegralDomainLaws` bridge from `MathlibBridge.lean`, and deleted the legacy duplicate owner/stub files rather than leaving compat surfaces behind. `python scripts/validate_structure.py --path LRA/AlgebraicStructures/IntegralDomain`, `lake build LRA.AlgebraicStructures.IntegralDomain`, `lake build LRA.VolumeVICore`, and `lake build LRA.VolumeVIExamplesFailures` all pass on 2026-08-31. |
| as-32 | 7 | LinearlyOrderedRing | **done** | OrderedRing | Audit confirmed `LinearlyOrderedRingLaws` stays `RingLaws + LinearOrderLaws + AdditionRespectsOrderLaws + MultiplicationRespectsOrderLaws`. Reorganized to `Interface/{Definitions,Laws}`, added subject-local Mathlib bridge plus `Int`/`Rat`/`Real` examples, and deleted legacy duplicate owner/stub files. |
| as-33 | 8 | Field | **done** | DivisionRing, CommutativeRing | Audit confirmed `FieldLaws` stays `DivisionRingLaws + MultiplicativeCommutativeLaws`. Reorganized to `Interface/{Definitions,Laws,ModelTheory}`, added a subject-local Mathlib bridge plus `Rat`/`Real`/`Complex` examples, retargeted minimal direct consumers, and deleted legacy duplicate owner/stub files. |
| as-34 | 9 | OrderedField | **done** | Field | Audit confirmed `OrderedFieldLaws` stays `FieldLaws + LinearOrderLaws + AdditionRespectsOrderLaws + MultiplicationRespectsOrderLaws`. Reorganized to `Interface/{Definitions,Laws,ModelTheory}`, added a subject-local Mathlib bridge plus `Rat`/`Real` examples, retargeted minimal direct consumers, and deleted legacy duplicate owner/stub files. |
| as-35 | 10 | CompleteOrderedField | **done** | OrderedField | Audit confirmed `CompleteOrderedFieldLaws` stays the established conjunction `OrderedFieldLaws R ∧ OrderCompletenessLaws R SetObject` (plain `abbrev`, not a class abbrev). Reorganized to `Interface/{Definitions,Laws,ModelTheory}`, added a subject-local Mathlib bridge theorem plus a direct `Real` example, retargeted minimal direct consumers, and deleted legacy duplicate owner/stub files. |

**Standing design (confirmed 2026-08-26, see DECISIONS.md D7):** carrier nonemptiness
is expressed as `[Nonempty R]`, an ambient instance-implicit parameter on every
`<X>Laws` class that needs it, never as a stored field — a stored field collides by
name the moment two such classes are combined by a later `class abbrev` (this bit
`AdditiveSemigroupLaws`/`MultiplicativeSemigroupLaws` at the Semiring tier). Applied
to Magma/Semigroup/CommutativeSemigroup/AdditiveSemigroup and propagated to all 22
downstream combining classes tiers 2–10. New `<X>Laws` classes going forward should
take `[Nonempty R]` as a parameter from the start, not a field.

**Standing rule (confirmed 2026-08-26, CORRECTED same day — see DECISIONS.md D8):**
theorem proofs are the user's to write — leave every new `theorem`/`instance`-of-a-
`Prop`-class `sorry`, with NO exceptions for Mathlib bridges, `ClosedSubset*Laws`
builders, or other "structural glue" — that exemption was a mistake I introduced
myself and the user corrected twice (first narrowly, then a second time after it let
a full order-theory proof through in as-08's `InducedOrder` construction). Only
*data* (`Mul`/`Add` instances, `Set`/`structure` definitions) is still written in
full. Proofs from *before* the as-03 correction (Magma/Semigroup's closure and
non-associativity proofs, as-01/02) stay as they are — confirmed by the user, not
reverted retroactively. Everything proved in as-03 through as-06 and as-08's
Constructions/Examples *was* reverted to `sorry` on 2026-08-26 to match this rule —
see each item's ledger notes for exactly what changed.

**Cross-workstream note (2026-09-01):** Landau Chunk 01 mechanically advanced the
existing `Interface/ModelTheory` surfaces for `as-18`, `as-19`, `as-20`, `as-22`, and
`as-25` ahead of their full per-subject queue pass. `as-18`, `as-19`, `as-20`, `as-22`,
`as-24`, `as-25`, `as-26`, `as-27`, `as-28`, `as-29`, `as-30`, `as-34`, and `as-35`
have now completed their full queue-local audit/examples/constructions pass as well;
the canonical queue is complete.

**Queue state:** the canonical per-subject algebraic-structures queue through
`as-35 CompleteOrderedField` is complete.

**Follow-on Queue: Algebraic Bridge Normalization (added 2026-09-01)**

| Item | Tier | Subject | Status | Depends on | Notes |
|---|---|---|---|---|---|
| nsc-01 | 1 | Naturals-family carrier ownership reset | **done** | as-35 | `NaturalNumbers` now owns the canonical `Landau` / `VonNeumann` / `Presburger` / `Mathlib` carrier choices directly; `PositiveNaturals`, `ZeroBasedNaturals`, `PresburgerArithmetic`, and `FirstOrderArithmetic` consume those owner choices instead of defining peer carrier surfaces, while `Carriers` remains only a compatibility aggregator pending `nsc-02`/`nsc-03`. `python scripts/validate_structure.py --path LRA/NumberSystems` and `lake build LRA.NumberSystems.NaturalNumbers LRA.NumberSystems.PositiveNaturals LRA.NumberSystems.ZeroBasedNaturals LRA.NumberSystems.PresburgerArithmetic LRA.NumberSystems.FirstOrderArithmetic LRA.NumberSystems.Carriers` pass on 2026-09-01. |
| nsc-02 | 2 | Retire redundant successor-adjacent number-system subjects | **done** | nsc-01 | Deleted the redundant subject surfaces `FirstOrderArithmetic`, `PresburgerArithmetic`, `PositiveNaturals`, and `ZeroBasedNaturals`; removed only the top-level `LRA.NumberSystems` / `Constructions` / `Examples` / `Interop` imports that kept them live; preserved `NaturalNumbers` as the canonical naturals-family owner and left `Carriers` as a compatibility aggregator. `python scripts/validate_structure.py --path LRA/NumberSystems` and `lake build LRA.NumberSystems` pass on 2026-09-01. |
| nsc-03 | 3 | Import and aggregator cleanup after number-system retirement | **done** | nsc-02 | Finished the post-retirement structural cleanup: kept the surviving top-level `LRA.NumberSystems` / `Constructions` / `Examples` / `Interop` routers limited to live subjects, retargeted `Carriers.Definition` to the canonical `NaturalNumbers` owner aliases instead of raw retired-subject-era naturals construction imports, and kept `Carriers` as a compatibility aggregator with the one explicit `Landau.Laws` import its witnesses now need after that transitive cleanup. `python scripts/validate_structure.py --path LRA/NumberSystems` and `lake build LRA.NumberSystems` pass on 2026-09-01. |
| ags-01 | 4 | Naturals-family construction algebraic bridges | **done** | nsc-03 | Added construction-local `Satisfy_AlgebraicStructures.lean` owners for the stable normalized `NaturalNumbers` constructions: `Landau` now exposes named bridges for `AdditiveCommutativeSemigroup`, `CommutativeMonoid`, and `CommutativeSemiringWithoutZero`, while `Mathlib` exposes named bridges for `AdditiveMonoid`, `CommutativeMonoid`, `Semiring`, `CommutativeSemiring`, and `OrderedSemiring`. `VonNeumann` and `Presburger` were left untouched intentionally because their current public construction surfaces still stop short of a stable algebraic-law package, so forcing bridge owners there would have broadened scope into construction development rather than bridge normalization. `python scripts/validate_structure.py --path LRA/NumberSystems/NaturalNumbers` and `lake build LRA.NumberSystems.NaturalNumbers` pass on 2026-09-01. |
| ags-02 | 5 | WholeNumbers and Integers construction algebraic bridges | **done** | ags-01 | Added construction-local `Satisfy_AlgebraicStructures.lean` owners for the stable `WholeNumbers` and `Integers` constructions: `WholeNumbers.Landau` now exposes named bridges for `CommutativeSemiring` and `OrderedSemiring`; `WholeNumbers.Mathlib` exposes named bridges for `AdditiveMonoid`, `CommutativeMonoid`, `Semiring`, `CommutativeSemiring`, and `OrderedSemiring`; `Integers.Mathlib` and `Integers.Polish.TwoSidedSuccessor` expose named bridges for `AbelianGroup`, `Ring`, `CommutativeRing`, `IntegralDomain`, and `OrderedRing`; and `Integers.QuotientOrderedPairs` exposes named bridges for `IntegralDomain` and `OrderedRing`. `Tao`, `Mendelson`, and `Pfefer` were left untouched intentionally because they do not yet expose stable construction-local algebraic-law packages comparable to those owners. `python scripts/validate_structure.py --path LRA/NumberSystems/WholeNumbers`, `python scripts/validate_structure.py --path LRA/NumberSystems/Integers`, `lake build LRA.NumberSystems.WholeNumbers`, and `lake build LRA.NumberSystems.Integers` pass on 2026-09-01. |
| ags-03 | 6 | RationalNumbers and RealNumbers construction algebraic bridges | **done** | ags-02 | Added construction-local omnibus owners for the stable scalar-chain constructions. `RationalNumbers.Mathlib` and `RationalQuotientFractions` expose `Field` and `OrderedField`; `RealNumbers.Mathlib`, `Cauchy`, `Cantor`, `Dedekind`, `PrimitiveIntervals`, and `EffectiveCauchy` expose `Field`, `OrderedField`, and `CompleteOrderedField`. `Dyadic` remains deferred because it lacks a stable construction-local ordered-field/completeness package. `python scripts/validate_structure.py --path LRA/NumberSystems/RationalNumbers`, `python scripts/validate_structure.py --path LRA/NumberSystems/RealNumbers`, `lake build LRA.NumberSystems.RationalNumbers`, and `lake build LRA.NumberSystems.RealNumbers` pass on 2026-09-01. |
| ags-04 | 6 | ComplexNumbers and GaussianIntegers construction algebraic bridges | **done** | ags-02 | Added construction-local omnibus owners for the stable complex and Gaussian-integer constructions. `ComplexNumbers.Mathlib` exposes `Field`; `ComplexNumbers.OrderedPairs` exposes `CommutativeRing` and `Field`; `GaussianIntegers.OrderedPairs` exposes `AbelianGroup`, `Ring`, and `CommutativeRing`. No `GaussianIntegers` generic bridge or stronger ordered/domain targets were forced, since those stable public law packages are still absent. `python scripts/validate_structure.py --path LRA/NumberSystems`, `lake build LRA.NumberSystems.ComplexNumbers`, and `lake build LRA.NumberSystems.GaussianIntegers` pass on 2026-09-01. |
| ags-05 | 7 | Late-tier model-theory reduct normalization | **done** | as-35 | Added promoted model owners plus direct `Interface/ModelTheory` forgetful surfaces for the late-tier pairs that already had both model surfaces: `CompleteOrderedField -> OrderedField`, `OrderedField -> Field`, `Field -> DivisionRing`, `DivisionRing -> Ring`, `OrderedRing -> Ring`, `CommutativeRing -> Ring`, and `IntegralDomain -> CommutativeRing`. Kept the work owner-local to `Interface/ModelTheory/Model.lean`, with no `Interface/Laws` inheritance wrappers or compat layers introduced. `lake build LRA.AlgebraicStructures` passes on 2026-09-01. |
| nsc-04 | 8 | Discuss Peano cleanup boundary | **done** | ags-05 | Corrected boundary: `PeanoSystem` owns the abstract `base`/`successor` axioms, induction, predecessor theory, recursion, and categoricity. `SuccessorArithmetic` consumes that system, names `zero = base`, and derives `one = successor zero`; duplicate successor-only signature/model-theory/UA surfaces were deleted. No wrappers, shims, or compatibility layers were created. |
| nsc-05 | 9 | Peano-base downstream repair | **done** | nsc-04 | Retargeted every live downstream consumer of the removed `PeanoSystem.one` field to `base`, beginning with NaturalNumbers Landau and continuing through PeanoSystem core theory, WholeNumbers Landau, NaturalNumbers Landau/VonNeumann/Presburger constructors, and Carriers witnesses. The final WholeNumbers Landau well-foundedness Peano record and its source renderings now use `base`. No compatibility alias/shim/wrapper was created. `lake build LRA.NumberSystems` passes on 2026-09-01; the structural validator remains non-discriminating because of the pre-existing missing `SuccessorArithmetic` interface files. |
| nsc-06 | 10 | SuccessorArithmetic additive monoid | not started | nsc-05 | Give `SuccessorArithmetic` one canonical Peano-recursive addition owner with `zero` as identity, then expose `AdditiveMonoidLaws`; retarget Landau to consume it rather than duplicate it. All proof-carrying declarations stay `sorry`. |
| nsc-07 | 11 | Scalar carrier ownership completion | not started | nsc-05 | Move canonical and Mathlib aliases for `N/W/Z/Q/R` into their owning subjects; each construction owns `Carrier`, while `Carriers` becomes a compatibility re-export only. Audit existing generic bridges without inventing new models. |
| nsc-08 | 12 | Generic signed and positive refinements | not started | nsc-07 | Add subject-local generic refinement subtypes: `N+`; `Z+`, `Z0+`, `Z-`, `Z0-`; `Q+`, `Q0+`, `Q-`, `Q0-`; and `R+`, `R0+`, `R-`, `R0-`. They derive from parent models, not new peer models. All theorems and proof-carrying instances stay `sorry`. |
| nsc-09 | 13 | Mathlib refinement aliases and bridges | not started | nsc-08 | Alias Mathlib carriers (`PNat`, `Nat`, `NNRat`, `NNReal`, and units where strict positivity is multiplicative) and add construction-local `Satisfy_Refinement` bridges into the generic subtype owners. All proof-carrying bridges stay `sorry`. |
| nsc-10 | 14 | Extended-real consolidation | not started | nsc-09 | Settle one canonical owner for existing `RealNumbers.Extended`/nonnegative material versus the design-only `ExtendedReal` directory; promote full, nonnegative, and nonpositive extended reals with `EReal`/`ENNReal` aliases, order-first bridges, and explicit arithmetic failure modes. All theorems and proof-carrying instances stay `sorry`. |

**Next follow-on step:** `nsc-06` gives `SuccessorArithmetic` the canonical
Peano-recursive additive-monoid owner.

**Open items, order-theoretic bridges (as-16, as-17):** neither `BoundedLattice` nor
`DistributiveLattice` has an algebra↔order bridge, unlike `Lattice`'s own (found pre-built
at `LRA/Order/Interop/AlgebraicLattice.lean`). `BoundedLattice`'s order-theoretic
*definition* doesn't exist either (would need building from `LRA/Order/Bounds/`'s
`TopElement`/`BottomElement`); `DistributiveLattice`'s order-theoretic definition DOES
already exist (`LRA.Order.Lattices.DistributiveLattice`, already `sorry`'d) — only the
bridge connecting it to the algebraic side is missing. Neither was built since the user's
own messages for those two items didn't include an explicit equivalence-of-definitions
derivation the way `JoinSemilattice`/`MeetSemilattice`/`Lattice`'s did. Worth checking the
same way for `BooleanAlgebra` before assuming its own order-theoretic side is already
covered or needs building from scratch — this pattern (check `LRA/Order/` first) has paid
off three times running now. Also worth checking existing Mathlib instances before
building a from-scratch example — `CommutativeMonoid`'s Boolean-conjunction example
turned out to be free via Mathlib's pre-existing `BooleanRing Bool` instance rather than
needing new work. `DistributiveLattice`'s down-set-lattice example (one of the user's 4
standard examples) was also skipped for scope — a genuine from-scratch poset/down-set
construction, more substantial than reuse-driven examples elsewhere. Nonemptiness on any new atomic `<X>Laws` class must be
`[Nonempty R]`, never a stored field, per D7. Monoid (as-10) built this whole process's
first-ever `Failures/` folder (D3 planned the pattern from the start — "a semigroup with
no identity is exactly Monoid's failure mode" — but no prior item, including Semigroup
itself, had actually built one yet); future subjects with an obvious tier-below failure
mode should follow that same reuse pattern rather than restating content. Open items: a
non-Archimedean `Failures/` example was deferred from as-06 (see its ledger notes). Both
JoinSemilattice and MeetSemilattice have their order-theoretic bridge built
(`Constructions/OrderTheoretic/InducedOrder.lean`, sorry'd per D8). DiscreteInteger
(as-07) surfaced a real D2-class audit bug (acyclicity checked only at the basepoint
instead of universally) — fixed; see its ledger notes. As-08 also needed a follow-up
correction this session (`Examples/UnionSemilattice.lean` had one theorem the original D8
sweep missed, left fully proved) — fixed, `sorry`'d, re-verified clean. Worth a close read
of any subject's Examples/Constructions files before trusting a "done" status's D8
compliance claim at face value, since this is now a repeat miss (see DECISIONS.md D8's own
text about repeat corrections). Also worth noting for future Mathlib-bridge work: the
pre-existing, untouched (tier 4+, predates this repair process) `MathlibBridge.lean` uses
unsorried `:= ⟨⟩` for its own class-abbrev combining instances (Semiring/Ring/Field/etc.)
— do NOT treat that file as precedent when building a *new* item's own combining bridge;
Monoid's own bridge was sorry'd despite being an equally "trivial" combination, per D8's
explicit no-exceptions text.
