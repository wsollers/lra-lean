# as-05 — AdditiveCommutativeSemigroup

**STATUS: done, 2026-08-26.**

## What was found and fixed

Same gap as-03 found on the multiplicative side: `AdditiveCommutativeLaws` (this
subject's only pre-existing `Laws` content) is atomic — commutativity alone, no
associativity, and nothing combined it with `AdditiveSemigroupLaws`. Checked every
downstream user (`AbelianGroupLaws`, `Semiring/CommutativeSemiringWithoutZero
Laws`) — each already lists `AdditiveSemigroupLaws R` *alongside*
`AdditiveCommutativeLaws R` separately, so the atomic mixin being
commutativity-only is correct and deliberate, same reasoning as as-03. Left
`AdditiveCommutativeLaws` untouched.

Added `AdditiveCommutativeSemigroupLaws` (`class abbrev ... := AdditiveSemigroupLaws
R, AdditiveCommutativeLaws R`) to `Interface/Laws/Definition.lean`, with
`[Nonempty R]` in its own header per D7 (needed because `AdditiveSemigroupLaws`
requires it).

## What was built

- `Interface/Definitions/ConceptSignature.lean` (unchanged content — additive
  commutative semigroups share `AdditiveSemigroup`'s operation signature),
  `Interface/Laws/Definition.lean` — no `ModelTheory/` folder (none existed before
  the reorg, matching Magma/AdditiveSemigroup's precedent).
- `Constructions/Set/ClosedSubset.lean` — `ClosedSubsetAdditiveCommutativeSemigroupLaws`:
  commutativity transfers through a closed subset for free, built on
  `AdditiveSemigroup`'s associativity transfer rather than restating it.
- `Constructions/Mathlib/Laws.lean` — bridge instance from `[AddCommSemigroup R]
  [Nonempty R]`.
- `Examples/` (one file per example):
  - `PositiveNaturalsUnderAddition.lean` — reuses `AdditiveSemigroup.Examples`'
    bare-`Nat` carrier and closure proof directly (no wrapper struct — `Nat`'s
    native `+` already is the operation); adds `AdditiveCommutativeLaws Nat`
    (`Nat.add_comm`).
  - `PositiveEvenNaturalsUnderAddition.lean` — reuses `AdditiveSemigroup.Examples`'
    Nat-based even-naturals example directly; no new commutativity instance needed,
    it comes from the file above.
  - `OpenIntervalUnderAddition.lean` — `(ℝ_{>0}, +)`, total `ℝ` carrier via the
    Mathlib bridge (`Real` is a Mathlib `AddCommSemigroup`), no wrapper struct.
    Needed explicit `show (0:Real) < ... from ...` casts since `PositiveReals`'s
    set-builder membership doesn't unfold automatically against `add_pos`/`one_pos`.
  - All law-instance/closure proofs left proved, matching as-01–04 precedent; no
    "interesting fact" theorem was called for by this item's spec, so none was
    added.
- Import-path fixes: `AbelianGroup/Semiring/CommutativeSemiringWithoutZero`'s
  `Laws/Definition.lean` (`.Laws.Definition` → `.Interface.Laws.Definition`),
  `VolumeVICore.lean` (`.Laws` → `.Interface.Laws`), `VolumeVIExamplesFailures.lean`
  (dropped the now-deleted `.Laws.Examples` stub import).

Gates: `validate_structure.py` on `AdditiveCommutativeSemigroup` and the full
`AlgebraicStructures` tree both pass. `lake build` on AdditiveCommutativeSemigroup,
its Examples, all three downstream `Laws` consumers, `VolumeVICore`,
`VolumeVIExamplesFailures`, and the full `AlgebraicStructures` umbrella — 1919 jobs,
clean.

Not committed yet (only commit when asked, per this session's convention).

## Addendum, 2026-08-26: proofs reverted per D8

The "all law-instance/closure proofs left proved" line above was wrong — see
`DECISIONS.md` D8. `ClosedSubsetAdditiveCommutativeSemigroupLaws`, the Mathlib
bridge instance, and the `AddCommutative`/closure/`Nonempty`-witness proofs across
the Examples files were reverted to `sorry`. Re-verified clean.
