name: Lean Docker Build and Check

on:
  push:
    branches: [main, develop]
    paths:
      - 'LRA/**'
      - 'scripts/**'
      - 'Dockerfile'
      - 'Makefile'
      - 'build.ps1'
      - 'lakefile.lean'
      - 'lean-toolchain'
      - 'lake-manifest.json'
      - '.github/workflows/lean-ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'LRA/**'
      - 'scripts/**'
      - 'Dockerfile'
      - 'Makefile'
      - 'build.ps1'
      - 'lakefile.lean'
      - 'lean-toolchain'
      - 'lake-manifest.json'
      - '.github/workflows/lean-ci.yml'
  workflow_dispatch:

jobs:
  build:
    name: Docker Lake Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Lean Docker image
        run: docker build --quiet --target lean-build -t lra-lean .

      - name: Check no Mathlib import in VolumeI / VolumeII
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            lra-lean \
            bash -lc 'if grep -rn "^import Mathlib" LRA/VolumeI/ LRA/VolumeII/ LRA/VolumeI.lean LRA/VolumeII.lean --include="*.lean"; then
              echo "ERROR: Mathlib import found in VolumeI/VolumeII (must be bare Lean 4)."
              exit 1
            fi
            echo "No Mathlib imports in VolumeI / VolumeII"'

      - name: Enforce Volume II namespace policy
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            lra-lean \
            bash -lc 'if grep -R -n -E "^namespace Peano([[:space:]]|$)|^open Peano([[:space:]]|$)|Peano\.[A-Za-z_]" LRA --include="*.lean"; then
              echo "ERROR: legacy Peano namespace declaration, open command, or qualified reference found."
              exit 1
            fi
            echo "All Peano-system and natural-number declarations use the LRA.VolumeII hierarchy."'

      - name: Report proof placeholders in VolumeI / VolumeII
        continue-on-error: true
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            lra-lean \
            bash -lc 'if grep -rn "\bsorry\b" LRA/VolumeI/ LRA/VolumeII/ --include="*.lean"; then
              echo "::warning::sorry found in VolumeI/VolumeII — statements compile, proofs remain pending."
            else
              echo "No sorry in VolumeI / VolumeII"
            fi'

      - name: Build active Lean libraries
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            lra-lean \
            bash -lc 'lake build LRAVolumeI LRAVolumeII > /tmp/lake-build.log 2>&1 || {
              status=$?
              echo "Lean build failed. Final diagnostics:"
              tail -n 180 /tmp/lake-build.log
              exit $status
            }
            tail -n 40 /tmp/lake-build.log'

      - name: Lint doc-comments
        continue-on-error: true
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            lra-lean \
            bash -lc 'if [ -f scripts/lint_doccomments.sh ]; then
              bash scripts/lint_doccomments.sh LRA/VolumeII
            fi'

      - name: Print proof statistics
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            lra-lean \
            bash -lc 'find LRA -name "*.lean" | sort | while read f; do
              thms=$(grep -c "^theorem\|^lemma\|^corollary" "$f" 2>/dev/null || echo 0)
              defs=$(grep -c "^def\|^noncomputable def" "$f" 2>/dev/null || echo 0)
              echo "  $f : $thms theorems, $defs defs"
            done'
