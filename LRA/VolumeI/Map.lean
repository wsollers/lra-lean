import LRA.Function
import LRA.VolumeI.Map.All

/-!
Volume-I compatibility/curricular aggregate for historical `Map` paths.

Canonical function mathematics is owned by `LRA.Function`. The remaining
`LRA.VolumeI.Map` modules are compatibility aliases, concrete set-backed
adapters, examples, or concepts awaiting migration to a more appropriate
later subject (for example morphisms).
-/
