import LRA.VolumeI.Logic.Semantics.TermEvaluation
import LRA.VolumeI.Logic.Semantics.Assignment
import LRA.VolumeI.Logic.Semantics.Satisfaction
import LRA.VolumeI.Logic.Semantics.Examples

/-!
Aggregate import shim for the `Semantics` layer (`Semantics/`): term
evaluation, assignment updating, first-order formula satisfaction, and the
second end-to-end checkpoint (`A ∧ B`, both true, via nullary relation
symbols).
-/
