import LRA.VolumeI.Logic.Language.Symbols
import LRA.VolumeI.Logic.Language.FirstOrder
import LRA.VolumeI.Logic.Language.SecondOrderMonadic
import LRA.VolumeI.Logic.Language.Notation
import LRA.VolumeI.Logic.Language.Propositional
import LRA.VolumeI.Logic.Language.Examples

/-!
Aggregate import shim for the `Language` layer as a whole: arity-indexed
symbol sets (`Language/Symbols`), the first-order signature
(`Language/FirstOrder`), the monadic second-order language alias
(`Language/SecondOrderMonadic`), the shared logical symbols
(`Language/Notation`), propositional languages (`Language/Propositional`),
and worked examples of concrete signatures (`Language/Examples`).
-/
