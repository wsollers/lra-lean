import LRA.VolumeI.Logic.Language.Symbols
import LRA.VolumeI.Logic.Language.Notation
import LRA.VolumeI.Logic.Language.Propositional
import LRA.VolumeI.Logic.Language.Examples

/-!
Aggregate import shim for the `Language` layer as a whole: the similarity
type (`Language/Symbols`), the shared logical symbols (`Language/Notation`),
propositional languages (`Language/Propositional`), and worked examples of
concrete signatures (`Language/Examples`).
-/
