import LRA.VolumeI.Logic.Syntax.Term
import LRA.VolumeI.Logic.Syntax.Propositional
import LRA.VolumeI.Logic.Syntax.FirstOrder
import LRA.VolumeI.Logic.Syntax.SecondOrderMonadic

/-!
Aggregate import shim for the `Syntax` layer (`Syntax/`): terms,
propositional formulas with their truth-table semantics
(`Syntax/Propositional`), first-order formulas (`Syntax/FirstOrder`), and
monadic second-order formulas (`Syntax/SecondOrderMonadic`). Free
variables, substitution, and subformula are added here as they are built.
-/
