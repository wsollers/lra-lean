# Absorption Sources

This family records the two-operation absorption laws used in lattice-style
algebraic presentations:

```text
join a (meet a b) = a
meet a (join a b) = a
```

The name is intentionally distinct from `Absorbing`, which records
element-level absorbers for one operation.
