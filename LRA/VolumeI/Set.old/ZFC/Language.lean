import LRA.VolumeI.Set.ZFC.Language.Signature

/-!
Aggregate import shim for `Set/ZFC/Language/`: the signature of ZFC -- a
single binary relation symbol `∈`, with no function symbols and no
constants -- the one non-logical choice that fixes ZFC as a first-order
theory over `Logic.FirstOrder`'s machinery.
-/
