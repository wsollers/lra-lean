namespace LRA.VolumeI.Set

/-!
`ZFCTreeSet`'s native operations.

Status: placeholder. Nothing to state yet -- `ZFCTreeSet` itself has no
`Member` relation or operations defined (see `ZFCTreeSet/ZFCTreeSet.lean`).
Once the pre-set tree type and its quotient exist, this file gets a
`NativeSetOperationsModel` instance built by direct recursive
construction, proved to satisfy the `Is___Of` specifications directly
rather than via `Classical.choose` the way `ZFCSet`'s will.
-/

end LRA.VolumeI.Set
