import LRA.EuclideanSpace.RealLine.Definition
import LRA.EuclideanSpace.RealLine.Theorems

/-!
Aggregate import for the `RealLine` concept (Lecture 1): the real
continuum, distance, intervals and rays, and the translation/dilation
transformations. `RealLineDistance`, the four bounded interval variants,
the four unbounded ray variants, and `Translate1`/`Dilate1` live in
`RealLine/Definition.lean`; their basic properties live in
`RealLine/Theorems.lean`.
-/
