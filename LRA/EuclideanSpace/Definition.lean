import LRA.NumberSystems.RealNumbers

namespace LRA.EuclideanSpace

/-!
The subject's core notion -- not yet decided. See `LRA/EuclideanSpace.lean`
for the agreed scope (ℝ → ℝ² → ℝ³; coordinates, points, lines, planes,
surfaces, intervals; not normed/vector/metric space per se).

Placeholder per §2.3 of `subject-architecture-prompt.md`: an empty role
file naming its current chain tail (`LRA.NumberSystems.RealNumbers`, the
`RealModel` this whole subject will build on) plus its doc header, so the
module is well-formed and the router can import it by name. Not a stand-in
for moved content -- there is no prior version of this subject.
-/

end LRA.EuclideanSpace
