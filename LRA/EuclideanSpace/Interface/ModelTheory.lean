import LRA.EuclideanSpace.Interface.ModelTheory.Model

/-!
Aggregate import for the `Interface.ModelTheory` concept: registering a
concrete `Point` realization as a formal `L_geom`-structure.
`BuildTarskiModel` lives in `ModelTheory/Model.lean`.
-/
