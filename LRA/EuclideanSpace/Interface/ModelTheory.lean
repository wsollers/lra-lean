import LRA.EuclideanSpace.Interface.ModelTheory.Structure
import LRA.EuclideanSpace.Interface.ModelTheory.Theory
import LRA.EuclideanSpace.Interface.ModelTheory.Model

/-!
Aggregate import for the `Interface.ModelTheory` concept:

* `ModelTheory/Structure.lean` exposes the Euclidean `L_geom`-structure
  layer (`TarskiStructure`, `BuildTarskiStructure`/`BuildTarskiModel`);
* `ModelTheory/Theory.lean` exposes first-order Euclidean theories and
  the satisfaction relation;
* `ModelTheory/Model.lean` explains the full structure/theory/model
  split.
-/
