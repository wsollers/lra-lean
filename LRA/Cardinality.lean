import LRA.Cardinality.Definition
import LRA.Cardinality.Theorems
import LRA.Cardinality.Properties

/-!
Cardinality: equinumerosity, domination, finiteness, countability, and
Cantor's theorem -- comparing the sizes of types by the functions that
exist between them (§1.2's arrow presentation), independent of any
particular `LRA.Set` backend.
-/
