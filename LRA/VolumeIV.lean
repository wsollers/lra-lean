/-
  ============================================================
  Volume IV — placeholder root
  ============================================================
  Scaffolded with a sentinel theorem so `lake build LRAVolumeIV`
  gates this volume. Replace with chapter imports as Volume IV is
  developed.
  ============================================================
-/

namespace LRA.VolumeIV

/-- **[Theorem — Volume IV Wiring]** Trivial sentinel confirming the
    Volume IV library compiles and is gated by CI. -/
theorem volume_iv_is_wired : True := trivial

end LRA.VolumeIV
