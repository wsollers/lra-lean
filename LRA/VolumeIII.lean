import LRA.VolumeIIIAll

/-!
Backward-compatible Volume III aggregate.

Prefer `LRA.VolumeIIICore` or `LRA.VolumeIIIExamplesFailures` when you want an
explicit split. `LRA.VolumeIII` remains the maximal Volume III surface.
-/
