import LRA.VolumeTBDAll

/-!
Backward-compatible provisional volume aggregate.

Prefer `LRA.VolumeTBDCore` or `LRA.VolumeTBDExamplesFailures` when you want an
explicit split. `LRA.VolumeTBD` remains the maximal provisional surface.
-/
