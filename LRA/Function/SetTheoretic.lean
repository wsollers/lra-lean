import LRA.Function.SetTheoretic.Definition
import LRA.Function.SetTheoretic.Theorems
import LRA.Function.SetTheoretic.Relationships

/-!
Opt-in set-theoretic realization of the canonical Function subject.

The default `LRA.Function` root remains backend-neutral. This module packages
functions whose domain, codomain, and graph are represented by backend set
objects and ordered-pair membership.
-/
