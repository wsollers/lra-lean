import LRA.NumberSystems.RationalNumbers.Constructions.RationalQuotientFractions

/-!
Aggregate import for rational-number constructions. Currently a single
construction (`RationalQuotientFractions`); `QuotientFractionsComparison`,
`Reduced`, and `FractionField` in `RationalNumbers/ComparisonModels.lean`
are thinner alternate-model existence claims, not full constructions run
through the §1.6.1 pipeline -- see the note there.
-/
