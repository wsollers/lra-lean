import LRA.NumberSystems.RationalNumbers.Definition

/-!
Backward-compatible import route for `RationalNumberSystem`.

The canonical aggregate route for the subject is
`LRA.NumberSystems.RationalNumbers`, but some downstream modules still import
this historical file name directly.
-/
