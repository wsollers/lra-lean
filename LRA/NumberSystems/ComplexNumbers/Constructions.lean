import LRA.NumberSystems.ComplexNumbers.Constructions.OrderedPairs

/-!
Aggregate import for complex-number constructions. Currently a single
construction (`OrderedPairs`): there is no rival axiomatization of the
complex numbers to reconcile the way Cauchy sequences and Dedekind cuts
compete to construct the reals, so a second construction is unlikely to
appear here, but the directory follows the same layout as every other
number system in this migration for consistency.
-/
