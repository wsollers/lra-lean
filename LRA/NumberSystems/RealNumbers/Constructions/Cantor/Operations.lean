-- LRA/NumberSystems/RealNumbers/Constructions/Cantor/Operations.lean
-- Placeholder pipeline stage: the source defines no arithmetic operations
-- (addition, negation, multiplication, etc.) on nested-interval
-- representatives or on `Carrier`.

import LRA.NumberSystems.RealNumbers.Constructions.Cantor.WellFoundedness

namespace LRA.NumberSystems.RealNumbers.Cantor

/-!
`LRA/VolumeII/RealNumbers/Cantor.lean` defines only the pre-carrier data
(`EndpointInterval`, `NestedIntervalSequence`), endpoint equivalence, the
resulting setoid and quotient carrier, and a comparison theorem against the
Cauchy construction. It states no representative-level arithmetic
operations for this stage. This file is therefore a deliberate placeholder,
present so later stages have a stage to import per §1.6.1. See
`RealNumbers/ProofOrder.md`.
-/

end LRA.NumberSystems.RealNumbers.Cantor
