-- LRA/NumberSystems/GaussianIntegers/Constructions/OrderedPairs/Behavior.lean
-- No new content at this stage: the source has no embedding-theorems
-- section (no `of_real`-style embedding is defined for `GaussianInteger`,
-- unlike `ComplexNumbers`) and no algebraic-closure-style obligation (the
-- Gaussian integers are a ring, not a field -- there is no notion of
-- "every nonconstant polynomial has a root" to state over a ring that
-- is not algebraically complete even informally).

import LRA.NumberSystems.GaussianIntegers.Constructions.OrderedPairs.Laws

namespace LRA.NumberSystems.GaussianIntegers.Constructions.OrderedPairs

end LRA.NumberSystems.GaussianIntegers.Constructions.OrderedPairs
