import LRA.NumberSystems.Integers.Constructions.QuotientOrderedPairs
import LRA.NumberSystems.Integers.Constructions.Tao
import LRA.NumberSystems.Integers.Constructions.Mendelson
import LRA.NumberSystems.Integers.Constructions.Polish
import LRA.NumberSystems.Integers.Constructions.Pfefer

/-!
Aggregate import for integer constructions: `QuotientOrderedPairs` (the
default, per `LRA.VolumeII.Integers.ConstructionModels`'s former reference
to it), `Tao`, `Mendelson`, `Polish` (currently the switch-selected active
construction, see `LRA.VolumeII.Integers.Implementation`), and `Pfefer`
(placeholder only, no content yet).
-/
