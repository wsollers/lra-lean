import LRA.VolumeIIAll

/-!
Backward-compatible Volume II aggregate.

Prefer `LRA.VolumeIICore` or `LRA.VolumeIIExamplesFailures` when you want an
explicit split. `LRA.VolumeII` remains the maximal Volume II surface.
-/
