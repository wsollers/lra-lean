import LRA.VolumeI.Function

/-!
Canonical import route for the typed and set-theoretic notions of function.

`LRA.Function` is the typed function abbreviation and also the durable owner of
function concepts: graphs, domains, codomains, images, preimages, fibers,
composition, restriction, extension, inverses, injectivity, surjectivity,
bijectivity, sections, retractions, products, partial functions, morphisms, and
the foundational set-theoretic function triple.
-/
