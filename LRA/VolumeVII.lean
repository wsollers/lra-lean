import LRA.VolumeVIIAll

/-!
Backward-compatible Volume VII aggregate.

Prefer `LRA.VolumeVIICore` or `LRA.VolumeVIIExamplesFailures` when you want an
explicit split. `LRA.VolumeVII` remains the maximal Volume VII surface.
-/
