import LRA.Set.TG.Axioms
import LRA.Set.TG.Public

/-!
Canonical TG theory layer.

`TG` owns the axioms and the mathematics derived from them. The primitive
single-sorted carrier and membership relation live in `LRA.Set.TGSet`; the law
certificates realizing this carrier against the set interface live in
`LRA.Set.TGSet.Laws`.
-/
