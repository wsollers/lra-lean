/-!
NBG satisfies the generic set API (LRA.Set.Interface). Instances only; the
theorems they package live in Laws/.

NBG does not currently claim any of the generic-API law classes
(ExtensionalityLaw, MembershipLaws, SeparationLaws, SymmDiffMembershipLaws,
PowersetMembershipLaws, CollectionMembershipLaws, IndexedMembershipLaws,
CountableMembershipLaws, UniversalMembershipLaws, UnionLaws,
IntersectionLaws, SubsetLaws, DifferenceLaws, SymmDiffLaws, ComplementLaws,
DistributivityLaws, PairingLaws): `NBGSet/Laws.lean` and
`NBGSet/Instances.lean` have no instances for any of them. This file is a
placeholder for when that changes.
-/
