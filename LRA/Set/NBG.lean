import LRA.Set.NBG.Axioms
import LRA.Set.NBG.Public

/-!
Canonical NBG theory layer.

`NBG` owns the axioms and the mathematics derived from them. The primitive
set/class carriers and membership relations live in `LRA.Set.NBGSet`; the law
certificates realizing the set carrier against the set interface live in
`LRA.Set.NBGSet.Laws`.
-/
