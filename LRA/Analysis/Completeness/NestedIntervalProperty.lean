import LRA.Analysis.Completeness.NestedIntervalProperty.Definition
import LRA.Analysis.Completeness.NestedIntervalProperty.Theorems
import LRA.Analysis.Completeness.NestedIntervalProperty.Failures

/-!
Aggregate import for the `NestedIntervalProperty` concept. The abstract
`NestedIntervalProperty` predicate and the concrete `IsNestedClosedIntervalFamily`
classification live in `NestedIntervalProperty/Definition.lean`; the nested
interval theorem and its consequences live in
`NestedIntervalProperty/Theorems.lean`; the closedness/boundedness necessity
counterexamples live in `NestedIntervalProperty/Failures.lean`.
-/
