import LRA.Analysis.Limits.Order.Theorems

/-!
Aggregate import for the `Order` concept: `≤`-preservation, the strict
converse, local boundedness near a limit point, and sign persistence near
the limit. The strict-inequality failure mode remains opt-in in
`Order/Failures`.
-/
