import LRA.VolumeI.Relations
import LRA.VolumeI.Map.Relation.All
import LRA.VolumeI.Map.Graph.All
import LRA.VolumeI.Map.SetTheoretic.All

/-!
Canonical import route for relations.

`LRA.Relation` owns named predicate relations, their laws, equivalence classes,
partitions, quotients, and well-foundedness. Generic relation/map interaction,
typed-map graphs, and set-theoretic relation triples live in the central
`LRA.Map` topic and are imported here as peer namespaces.
-/
