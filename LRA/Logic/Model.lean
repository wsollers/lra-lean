import LRA.Logic.Model.Model
import LRA.Logic.Model.Comparison
import LRA.Logic.Model.Propositional
import LRA.Logic.Model.SecondOrderMonadic

/-!
Aggregate import for the `Model` layer (`Model/`): the `Model`
structure itself, signature comparison (embeddings, reducts, expansions)
in `Model/Comparison`, propositional models (valuations and satisfaction)
in `Model/Propositional`, and Henkin models for monadic second-order logic
in `Model/SecondOrderMonadic`. Worked model examples remain opt-in in
`Model/Examples`.
-/
