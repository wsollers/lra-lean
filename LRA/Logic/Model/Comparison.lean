import LRA.Logic.Model.Comparison.SignatureEmbedding
import LRA.Logic.Model.Comparison.Reduct
import LRA.Logic.Model.Comparison.Expansion
import LRA.Logic.Model.Comparison.ModelEmbedding
import LRA.Logic.Model.Comparison.ModelIsomorphism
import LRA.Logic.Model.Comparison.ModelIsomorphismSymmetric
import LRA.Logic.Model.Comparison.Automorphism
import LRA.Logic.Model.Comparison.Examples

/-!
Aggregate import for `Model/Comparison/`: signature embeddings,
reducts, and expansions (cross-signature vocabulary comparison); model
embeddings and isomorphisms, including automorphisms (isomorphisms from a
model to itself) and the proof that isomorphism is symmetric
(same-signature domain-level comparison); and worked examples (the
`Z ↪ Q ↪ R` chain over `L_OrderedRing`).
-/
