import LRA.Logic.Language.Symbols
import LRA.Logic.Language.FirstOrder
import LRA.Logic.Language.SecondOrderMonadic
import LRA.Logic.Language.Notation
import LRA.Logic.Language.Propositional
import LRA.Logic.Language.Examples

/-!
Aggregate import for the `Language` layer as a whole: arity-indexed
symbol sets (`Language/Symbols`), the first-order signature
(`Language/FirstOrder`), the monadic second-order language alias
(`Language/SecondOrderMonadic`), the shared logical symbols
(`Language/Notation`), propositional languages (`Language/Propositional`),
and worked examples of concrete signatures (`Language/Examples`).
-/
