import LRA.VolumeIV.MetricSpaces.SetsInMetricSpaces.SetDistance

/-!
Metric-space distance-to-set material.

This module is the stable topic path for point-to-set and set-to-set distance
theorems. Existing declarations currently live under
`SetsInMetricSpaces.SetDistance` and are re-exported here while the Volume IV
layout is reorganized by topic.
-/
