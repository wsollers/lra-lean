import LRA.VolumeIV.MetricSpaces.SetsInMetricSpaces.SetDiameter

/-!
Metric-space diameter material.

This module is the stable topic path for set-diameter definitions and
theorems. Existing declarations currently live under
`SetsInMetricSpaces.SetDiameter` and are re-exported here while the Volume IV
layout is reorganized by topic.
-/
