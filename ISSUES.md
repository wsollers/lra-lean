# LRA Audit — Issues Found (Bad/Imprecise Statements)

Running list of correctness/precision problems found during the theorem-by-theorem
audit. These are things that are already *there* but stated imprecisely, mismatched,
or outright wrong — as opposed to `ADDITIONS.md`, which tracks things that are
*missing*. Nothing here has been auto-fixed except where explicitly noted (the
structural corruption/duplication bugs, fixed with your approval during the Bounds
pass — see the "Fixed" column).

Severity key: 🔴 genuine math error · 🟠 serious content mismatch (remark describes
a different theorem/fact than the one it's attached to) · 🟡 minor/stylistic
(narrower scope, dropped clause, dangling reference, copy-paste non-translation).

## Bounds chapter

| # | Severity | Location | Issue | Fixed? |
|---|----------|----------|-------|--------|
| 1 | — (structural) | `notes-suprema-infima.tex`, 14 nodes | Leftover uniqueness-of-infimum formula pasted verbatim into unrelated nodes' Failure-modes blocks, + 6 stray/mislabeled cross-references | ✅ Fixed (backup kept) |
| 2 | — (structural) | 6 files, 64 pairs total (`notes-algebra-of-supremum-infimum.tex` ×26, `notes-bound-algebra.tex` ×8, `notes-image-inverse-extrema.tex` ×8, `notes-archimedean-property.tex` ×8, `notes-axiom-of-completess.tex` ×2, `notes-density.tex` ×5) | Duplicated Failure-modes remark blocks (older plain version immediately followed by newer/better version) | ✅ Fixed (backups kept) |
| 3 | 🟡 | `notes-order-separation.tex` — `cor:no-gaps-in-r` **[relocated: `completeness` was promoted to its own chapter, `F:\repos\lra-volume-iii\...\completeness\`; re-verified 2026-07-28 against the reworked file]** | All remark blocks encode only the weaker "no Dedekind gap" fact, dropping the corollary's actual sharper content (a *unique* separator `c` with an exact interval-shape characterization of `L`/`U`) | Not fixed — confirmed still present after the rework |
| 4 | 🟡 | `notes-nested-interval-property.tex` — `lem:nested-closed-intervals-have-ordered-endpoints` **[relocated, re-verified 2026-07-28]** | Remarks only formalize the trivial one-step case (`n`, `n+1`), not the lemma's actual general `n ≤ m` conclusion | Not fixed — confirmed still present after the rework |
| 5 | 🟡 | `notes-nested-interval-property.tex` — `cor:nested-intervals-vanishing-length-have-unique-point` **[relocated, re-verified 2026-07-28]** | Remarks omitted the "Moreover, `x = sup{aₙ} = inf{bₙ}`" clause entirely | ✅ **Fixed** — the theorem box itself now states the "Moreover" clause, and a new dedicated sibling corollary `cor:nested-intervals-vanishing-length-have-equal-endpoint-limits` now carries that content as its own node |
| 6 | 🟡 | `notes-nested-interval-property.tex` — `cor:unique-point-in-nested-intervals-is-endpoint-limit` **[relocated, re-verified 2026-07-28]** | Dependency list omits "increasing bounded sequence converges to its sup," which the proof implicitly needs | Not fixed — confirmed still present after the rework |
| 7 | 🟡 | `notes-archimedean-property.tex` — `thm:archimedean-property` **[relocated, re-verified 2026-07-28]** | Remarks formalize only the `x = 1` special case (`∀x∃n, n>x`) instead of the theorem's own two-variable `nx > y` statement | Not fixed — confirmed still present after the rework |
| 8 | 🟠 | `notes-archimedean-property.tex` — `cor:archimedean-reciprocal` ("Reciprocal Sequence Converges to Zero") **[relocated, re-verified 2026-07-28]** | Every remark block instead restates the general Archimedean property (`nx > y`) — nothing about ε, N, or the limit anywhere. Looks like leftover content from a different node | Not fixed — confirmed still present, byte-for-byte, after the rework |
| 9 | 🟡 | `notes-archimedean-property.tex` — `lem:integer-part-lemma` **[relocated, re-verified 2026-07-28]** | Remarks state the fully general (all `x∈ℝ`, `m∈ℤ`) floor fact instead of this lemma's own `x≥0`/`ℕ₀`-restricted statement | Not fixed — confirmed still present after the rework |
| 10 | 🟡 | `notes-archimedean-property.tex` — `lem:archimedean-integer-part-lemma` **[relocated, re-verified 2026-07-28]** | Remarks use a shifted `⌊x⌋+1` convention instead of the theorem's own `m` (`⌊x⌋`) | Not fixed — confirmed still present after the rework |
| 11 | 🔴 | `notes-archimedean-property.tex` — `lem:integer-ceiling-lemma` **[relocated, re-verified 2026-07-28]** | Remarks state `x < m ≤ x+1`, which is **not equivalent** to the theorem's correct `m-1 < x ≤ m` — disagrees at integer `x`, giving the wrong ceiling (`m=x+1` instead of `m=x`) | Not fixed — **the one confirmed genuine math error in the whole audit is still live** after the rework |
| 12 | 🟡 | `notes-archimedean-property.tex` — `lem:integer-above-lemma` **[relocated, re-verified 2026-07-28]** | Remarks use `∃m∈ℤ` instead of the theorem's own `∃n∈ℕ` | Not fixed — confirmed still present after the rework |
| 13 | 🟠 | `notes-archimedean-property.tex` — `lem:unit-length-interval-contains-integer` **[relocated, re-verified 2026-07-28]** | Remarks state a different (also true) fact about open intervals of length >1, not the theorem's own `x ≤ m ≤ x+1` statement | Not fixed — confirmed still present after the rework |

## Sequences chapter

| # | Severity | Location | Issue | Fixed? |
|---|----------|----------|-------|--------|
| 14 | 🟡 | `notes-sequences-definitions.tex` — `def:real-sequence` | "Predicate reading" is tautological/circular: `Sequence(x_n,ℝ) := Sequence(x_n,ℝ)`, unlike every other Predicate reading in the corpus which actually unpacks the predicate | Not fixed |
| 15 | 🟠 | `notes-limits.tex` — `thm:uniqueness-of-limits` | Interpretation claims "the Hausdorff remark in the failure mode block is the forward pointer," but no such remark exists anywhere in that node's actual Failure-modes block — dangling reference to content that was apparently never written | Not fixed |
| 16 | 🟡 | `notes-limits.tex` — `thm:positive-limit-eventually-positive` | Dependency list omits `thm:strict-limit-separation-gives-eventual-order`, of which this theorem is a direct specialization | Not fixed |
| 17 | 🟠 | `notes-tails.tex` — `thm:convergence-of-tail`, `thm:convergence-by-domination` | Both nodes' `[Interpretation]` remarks open with formally displayed contrapositive/negation content that reads exactly like a misplaced `[Negated quantified statement]`/`[Negation predicate reading]` pair (both nodes are missing those remark kinds). Hand-verified both displaced formulas are mathematically correct — placement bug, not a math error | Not fixed |
| 18 | 🟡 | `notes-tails.tex` — `thm:convergence-of-tail` | "Predicate reading" only captures the convergence⟺convergence half, omitting the "Moreover, the limits agree" clause | Not fixed |
| 20 | 🟡 | `notes-monotonicity.tex` — `def:eventually-monotone-sequence` | Dependency list's `\hyperref[def:m-tail]{...}` cross-reference displays the wrong title — "Convergence of a Tail" (the title of `thm:convergence-of-tail`) instead of "M-Tail of a Sequence" (the actual title of the `def:m-tail` target it points to). The other two `def:m-tail` references in the same file use the correct title | Not fixed |
| 21 | 🟡 | `notes-subsequences.tex` — `thm:squeeze-passes-to-subsequences` | Dependency list cites `thm:sequence-squeeze-theorem`, but the theorem's conclusion is only about the inequality `a_σ(k)≤x_σ(k)≤b_σ(k)` persisting on a tail — no convergence claim — and only needs `thm:eventual-properties-pass-to-subsequences` (already cited, and sufficient alone). The squeeze-theorem citation looks backward: this theorem is naturally a lemma feeding a later "squeeze conclusion transfers to subsequences" result, not a consumer of the squeeze theorem | Not fixed |
| 22 | 🟡 | `notes-subsequences.tex` — `thm:eventual-properties-pass-to-subsequences`, `thm:frequent-properties-yield-subsequences` | Both theorems are stated for a fully abstract index property `P(n)` — no sequence `x_n` appears in either statement — but both "Predicate reading" remarks write `Eventually(x_n,P)` / `Frequently(x_n,P)`, introducing sequence notation that isn't part of the theorem (apparently copied from the sequence-specific template elsewhere in the file without stripping `x_n`) | Not fixed |
| 23 | 🟡 | `notes-divergence.tex` — `thm:unbounded-above-has-positive-infinity-subsequence`, `thm:unbounded-below-has-negative-infinity-subsequence` | Both proofs need "unboundedness (above/below) passes to every tail" to construct the index map (else a bounded tail plus the max of finitely many initial terms would bound the whole sequence) — neither dependency list states this fact | Not fixed |
| 24 | 🟡 | `notes-liminf-limsup.tex` — `thm:tail-infima-are-increasing` | Dependency list is asymmetric with its sibling `thm:tail-suprema-are-decreasing`: the suprema theorem correctly cites `thm:supremum-is-monotone-under-inclusion`, but this (dual) theorem cites only the bare `def:infimum` instead of the analogous infimum-monotone-under-inclusion theorem it actually needs | Not fixed |
| 25 | 🟡 | `notes-liminf-limsup.tex` — `thm:liminf-squeeze-under-eventual-order` | `[Interpretation]` remark runs on, after two blank lines, into what reads as independent section-closing exposition (worked example: limsup/liminf of `(-1)^n`; remark that both envelopes collapse to `L` for convergent sequences) — correct content, but doesn't belong to this specific theorem's interpretation and should likely have been its own block | Not fixed |
| 26 | 🟠 | `notes-cluster-values.tex` — `def:cluster-value-sequence` vs. `notes-subsequences.tex` — `def:subsequential-limit` (also recurs in `notes-cauchy.tex` — `thm:cauchy-convergent-subsequence-converges`) | Two a priori different definitions (epsilon/frequently-close vs. exists-a-convergent-subsequence) — proven equivalent by `thm:cluster-values-are-subsequential-limits` — both use the identical Predicate-reading operator name `IsClusterValue(x_n,L,R)`. The collision makes that very equivalence theorem's own Predicate reading collapse into the tautology `IsClusterValue ⟺ IsClusterValue`, losing the theorem's actual content; it also silently reappears wherever a later theorem's Predicate reading cites "subsequential limit" via the same name (e.g. `notes-cauchy.tex`). Recommended fix: give the two notions distinct predicate names | Not fixed |

## Functions chapter

| # | Severity | Location | Issue | Fixed? |
|---|----------|----------|-------|--------|
| 27 | 🟠 (likely structural) | `notes-algebra-of-functions.tex`, right after `\subsection*{Algebra of Functions}` | Orphaned `[Interpretation]` remark about open intervals/cluster points/closed-bounded sets — pure point-set-topology content unrelated to composition/injectivity/preimages, not attached to any node, reads like leftover paste from the later `subsets-real-line` section | Not fixed |
| 28 | 🟠 | `notes-algebra-of-functions.tex` — `prop:composition-surjective` | "Predicate reading" states only the definition of the conclusion (`Surjective(g∘f) :≡ ∀c∃a...`), dropping the hypothesis `Surjective(f)∧Surjective(g)` entirely — no longer encodes an implication, unlike sibling `prop:composition-injective`. Also missing the Negated/Negation-predicate/Failure-modes blocks its siblings have | Not fixed |
| 29 | 🟠 (structural) | `notes-algebra-of-functions.tex` — `prop:preimage-union-intersection` | Proposition has 3 parts (union/intersection/complement), but "Standard quantified statement" only covers parts (i)/(ii) — part (iii) is missing from it. Instead, the same non-negated derivation of part (iii) is pasted verbatim into all three of "Negated quantified statement," "Negation predicate reading," and "Failure modes" — none of which it belongs to. Also missing a "Predicate reading" block. Matches the Bounds-pass duplicated-block corruption pattern | Not fixed (left as report-only per instruction) |
| 30 | — (structural) | `notes-real-valued-functions.tex`, 11 pairs (22 of 28 total Failure-modes blocks in the file) — `def:function-bounded-above` (1423,1441), `def:function-bounded-below` (1512,1530), `def:function-bounded` (1601,1621), `def:function-maximum-point` (3315,3333), `def:function-minimum-point` (3433,3451), `def:function-increasing` (3595,3613), `def:function-strictly-increasing` (3685,3703), `def:function-decreasing` (3805,3823), `def:function-strictly-decreasing` (3894,3912), `def:function-monotone` (4016,4038), `def:function-constant` (4207,4224) | Duplicated Failure-modes remark blocks (older plain version immediately followed by newer/better version), same structural corruption pattern as Bounds-pass issue #2 and Functions-pass issue #29. Line numbers refer to the file as read this pass; 6 legitimate solo Failure-modes blocks also present (lines 867, 1748, 1948, 2772, 2894, 3246) and are not part of this finding | Not fixed (report-only per "leave it in issues and continue") |
| 31 | 🟡 | `notes-real-valued-functions.tex` — `thm:function-algebra-closure` | Dependency list cites only `def:pointwise-sum-of-functions` and `def:pointwise-minimum-of-two-functions`, but the theorem's conclusion covers all seven pointwise operations (sum, difference, product, scalar multiple, absolute value, maximum, minimum) — difference, product, scalar-multiple, absolute-value, and maximum are missing from the dependency list | Not fixed |
| 32 | 🟡 (organizational, not a math error) | `notes-real-valued-functions.tex` — `thm:bounded-away-quotient` vs. `cor:bounded-quotient-by-bounded-away-denominator` | The two nodes have IDENTICAL hypotheses and conclusions — the "corollary" is a verbatim restatement of the theorem, not a specialization or variant. Not a correctness error, but an odd organizational choice worth flagging | Not fixed |
| 33 | 🟡 | `notes-subsets-of-R.tex` — `def:adherent-point-r`, `def:isolated-point-r`, `def:bounded-set-r`, `thm:heine-borel-subsets-real-line` | "Negation predicate reading" is a verbatim duplicate of the "Negated quantified statement" immediately above it — no translation into set/predicate notation happens. New instance of the recurring pattern tracked as item 19, but hitting a different remark pair (Negation-predicate-reading ↔ Negated-quantified-statement instead of Predicate-reading ↔ Standard-quantified-statement). Contrast with `def:cluster-point-r` and `def:closed-set-r` in the same file, which translate correctly | Not fixed |
| 34 | — (structural) | `notes-subsets-of-R.tex` — `thm:heine-borel-subsets-real-line` | Two separate back-to-back `[Failure modes]` remark blocks (lines ~1034 and ~1057 as read this pass) — the classic duplicated-block corruption pattern (generic-Exposition version immediately followed by a more specific, better version). Every other node in this file has exactly one Failure-modes block | Not fixed (report-only per "leave it in issues and continue") |
| 35 | 🟡 | `notes-subsets-of-R.tex` — `lem:closure-elementary` | Theorem box states 4 parts — (i) `X⊆X̄`, (ii) `(X∪Y)‾=X̄∪Ȳ`, (iii) `(X∩Y)‾⊆X̄∩Ȳ`, (iv) monotonicity `X⊆Y⟹X̄⊆Ȳ` — but "Standard quantified statement" covers only (ii)/(iii), silently omitting (i) and (iv) entirely (not misplaced elsewhere, just dropped) | Not fixed |
| 36 | 🟠 (possible genuine gap — depends on this corpus's own `def:interval`, not yet read) | `notes-subsets-of-R.tex` — `cor:interval-all-limit-points` | States "if `I` is an interval, then every `x∈I` is a cluster point of `I`" with no nondegeneracy hypothesis. False for a degenerate single-point interval `I={a}` under conventions (e.g. Mathlib's `Set.OrdConnected`) that admit singletons as intervals — `a` would not be a cluster point of `{a}`. Moot if this corpus's own interval definition requires ≥2 points | Not fixed |

## Completeness chapter (new — was promoted from `bounding/notes/completeness` to its own top-level chapter during a rework; findings 3, 4, 6-13 above are the relocated/re-verified originals)

| # | Severity | Location | Issue | Fixed? |
|---|----------|----------|-------|--------|
| 37 | 🟡 | `notes-order-separation.tex` — `thm:dedekind-cut-property` (new theorem, added in the rework) | Theorem box concludes "either `L` has a maximum or `U` has a minimum," but the "Standard quantified statement" instead formalizes the WEAKER "∃c, a≤c≤b for all a,b" separator form — textually the same shape as the *previous* theorem's (`thm:order-separation-by-supremum`) own Standard block. Provably equivalent given the hypotheses (`L∪U=ℝ`, disjoint, strict order forces the separator into one side as its max/min), so not a math error, but it's not a translation of what this theorem's own box actually asserts | Not fixed |
| 38 | 🟠 (needs author judgment — possible circular dependency) | `notes-completeness-equivalences.tex` — `thm:lub-property-implies-monotone-convergence`, `thm:monotone-convergence-implies-lub-property` (both new) | Both theorems prove the ABSTRACT-FIELD version of the LUB-property ⟺ monotone-convergence equivalence (for an arbitrary Archimedean ordered field `F`), yet both cite `thm:monotone-convergence-theorem` (presumably the Sequences chapter's ℝ-specific Monotone Convergence Theorem) as a dependency. That looks backwards: the general abstract-field theorem is naturally what the concrete ℝ-specific one would specialize from/depend on, not the reverse — unless the Sequences-chapter theorem was proved by some entirely different route that doesn't presuppose LUB-equivalence, in which case this is fine and just needs a note. Flagging for your call, not asserting a hard error | Not fixed |
| 39 | 🟡 | `bounding/notes/bounds-extremals/notes-relative-bounds-suprema.tex` (new file, not yet wired into `index.tex`) — `def:relative-supremum-infimum` | Dependency list cites the ordinary `def:supremum`/`def:infimum`, which the formal definition doesn't use anywhere (relative supremum is defined independently via relative bounds, not as a specialization of ordinary supremum) — but omits `def:relative-bounds`, defined 50 lines earlier in the same file, which the definition explicitly builds on ("s is a relative upper bound of A in S") | Not fixed |

## Real Analysis chapter (Ch.1 — personal toolkit/reference, not a strictly linear theorem sequence; audited for exposition clarity and tool usefulness per author's explicit framing, in addition to correctness)

| # | Severity | Location | Issue | Fixed? |
|---|----------|----------|-------|--------|
| 40 | 🟡 (exposition clarity, not a math error) | `completeness-construction.tex` — `thm:inductive-selection`, "Proof strategy — the max device" remark | Parenthetical "Since $M_n<s$ (both entries are strictly below $s$ whenever $s\notin S$, or the constant sequence $x_n=s$ handles the case $s\in S$)..." is confusing and self-contradictory: a constant sequence $x_n=s$ is not strictly increasing, contradicting the theorem's own hypothesis. The special-casing is also unnecessary — if $(x_n)$ is strictly increasing in $S$ with $x_n\to s=\sup S$, no term can equal $s$ (else $x_{n+1}>x_n=s$ would exceed the supremum), so $M_n<s$ holds unconditionally regardless of whether $s\in S$. Recommend deleting the parenthetical entirely, or replacing with the one-line contradiction argument | Not fixed |
| 41 | 🟡 | `inequality.tex` — `thm:ineq-mixed-add` ("Mixed Addition of Inequalities") | Theorem states $a\le b\land c<d\Rightarrow a+c<b+d$, but "Predicate reading" swaps which variable pair is strict: $(a<b\land c\le d)\Rightarrow a+c<b+d$ — same overall shape, but attaches strictness to the wrong side | Not fixed |
| 42 | 🟠 | `inequality.tex` — `thm:ineq-reciprocal-positive` ("Reciprocal Reverses Strict Inequality for Positives") | Theorem states $0<a<b\Rightarrow 0<1/b<1/a$, but "Predicate reading" instead states the much weaker, unrelated fact $a>0\Rightarrow a^{-1}>0$ — doesn't translate the theorem's actual order-reversal content at all, only a positivity fact about a single reciprocal | Not fixed |
| 43 | 🟡 | `inequality.tex` — `thm:ineq-square-root-monotone` ("Square Root Is Monotone on Nonneg Reals") | Theorem states the nonstrict conclusion $0\le a\le b\Rightarrow \sqrt a\le\sqrt b$, but "Predicate reading" states the strict version $(0\le a<b)\Rightarrow \sqrt a<\sqrt b$ instead — that's the content of the sibling node `thm:ineq-square-root-strict-monotone`, not this one | Not fixed |

## Structure of the Real Line chapter (Ch.2)

| # | Severity | Location | Issue | Fixed? |
|---|----------|----------|-------|--------|
| 44 | 🟠 | `heine-borel/notes-heine-borel.tex` — chapter-opening `[Exposition]` remark for `thm:closed-bounded-interval-compact`/`thm:heine-borel` | The exposition explicitly says the general Heine–Borel statement "follows because a closed subset of a compact set is compact," but that lemma is never stated or proved anywhere in this chapter (checked all 14 sections in `\input` order) — the only two compactness theorems present are `thm:compact-implies-closed-bounded` (compact ⟹ closed∧bounded) and `thm:closed-bounded-interval-compact` (closed bounded interval ⟹ compact); neither is "closed subset of a compact set is compact." A load-bearing dependency is invoked by name in prose but never actually proved | Not fixed |

## Continuity chapter (Ch.9* — Book II, contains the function-limits content)

| # | Severity | Location | Issue | Fixed? |
|---|----------|----------|-------|--------|
| 45 | — (structural) | `limits/notes-limits.tex` — `def:limit-function`, `thm:limit-unique`; `limits/notes-one-sided-limits.tex` — `def:right-hand-limit`, `def:left-hand-limit`, `thm:two-sided-limit-iff-matching-one-sided-limits`; `point-continuity/notes-continuity.tex` — `def:relative-neighborhood`, `def:continuous-at-point`, `def:continuous-at-point-nbhd`, `def:continuous-at-point-seq`; `point-continuity/notes-discontinuity.tex` — `def:point-of-discontinuity`, `def:sequential-discontinuity-at-a-point`, `def:neighborhood-discontinuity-at-a-point`; `point-continuity/notes-oscillation.tex` — `def:oscillation-on-a-set`, `def:oscillation-at-a-point`, `thm:continuity-iff-zero-oscillation`, `prop:discontinuity-set-via-oscillation`; `global-theorems/notes-ivt.tex` — `def:bounded-on-a-set`, `thm:boundedness-theorem`, `def:absolute-extrema`, `thm:extreme-value-theorem`, `thm:location-of-roots`, `thm:bolzano-intermediate-value`; `uniform-continuity/notes-uniform-continuity.tex` — `def:uniform-continuity`, `thm:algebra-of-uniform-continuity-bounded`, `prop:sequential-uniform-continuity`, `def:lipschitz-condition`, `def:bi-lipschitz`; `monotone-functions/notes-monotone-functions.tex` — `cor:monotone-continuity-criterion`, `def:jump-of-function`; `monotone-functions/notes-limsup-functions.tex` — `def:limsup-liminf-function`, `prop:limsup-liminf-limit-criterion`; `gauge/notes-gauge.tex` — `def:interval-partition`, `def:tagged-partition`, `def:gauge`, `def:delta-fine-partition`, `thm:cousins-theorem`; `limits-at-infinity/notes-limits-at-infinity.tex` — `def:infinite-adherent-points`, `def:limit-at-infinity`; `gauge/notes-gauge.tex`'s duplication continues in `gauge/notes-tagged-partitions.tex` — `def:mesh-of-partition`, `def:refinement-of-partition`, `def:common-refinement`; `approximation/notes-approximation.tex` — `thm:step-function-approximation`, `def:piecewise-linear-function` (43 nodes — this pattern is now confirmed present in every section of the Continuity chapter: `limits`, `point-continuity`, `global-theorems`, `uniform-continuity`, `monotone-functions`, `limits-at-infinity`, `approximation`, `gauge`) | Duplicated `[Failure modes]` remark blocks (older plain-exposition version immediately followed by a fuller/annotated version with otherwise identical content) — same structural corruption pattern as Bounds #2, Functions #29/#30/#34. Appears to be a chapter-wide authoring artifact in Continuity, not isolated to one section. Note: `thm:preservation-of-intervals`, `thm:darboux-property`, and `thm:heine-cantor` each have only a single (non-duplicated) Failure-modes block, so the pattern is not universal even within one file | Not fixed (report-only per "leave it in issues and continue") |
| 46 | 🟠 | `limits/notes-limits.tex` — `thm:composition-of-limits` | Both of this theorem's `[Failure modes]` remark blocks display the generic single-function `∃ε₀>0 ∀δ>0 ∃x∈A (0<|x-c|<δ ∧ |f(x)-L|≥ε₀)` / `¬TendsTo(f,c,L,ℝ,ℝ)` formula — the failure condition for def:limit-function's own `f`, not anything about the composition `g∘f` this theorem is actually about. Reads like copy-paste from `def:limit-function`'s failure-mode block without adaptation to the composition context; the theorem's own genuine failure mode (the `g(c₂)=L` hypothesis being essential, illustrated correctly in the Exposition prose of the second block) is never captured formally | Not fixed |
| 47 | 🟠 | `point-continuity/notes-discontinuity.tex` — `def:types-of-discontinuity-at-a-point` | The three-way taxonomy (Removable / Jump / Essential) defines Essential simply as "not Removable," rather than as its own positive condition. Since Jump discontinuities are (by their own definition — the one-sided limits exist, are finite, but disagree, or agree but ≠ f(c)) also "not Removable," the stated definitions make Jump ⊆ Essential as written, not a disjoint third category alongside it. A reader relying on the definitions alone cannot conclude Jump and Essential are mutually exclusive, even though the surrounding exposition clearly intends a 3-way partition. Standard treatments instead define Essential (or "infinite"/"oscillatory") as its own positive condition — e.g. at least one one-sided limit fails to exist as a finite value — with Jump and Essential disjoint by construction. Should be tightened so Essential explicitly excludes Jump (e.g. "not Removable and not Jump," or a positive characterization) | Not fixed |
| 48 | 🔴 | `global-theorems/notes-ivt.tex` — `def:bounded-on-a-set`, Predicate reading block | After the correct `Bounded(f,A) := ∃M>0 ∀x∈A(|f(x)|≤M)` formula, a second, unrelated formula is pasted into the same remark block: `IsContinuous(f,[a,b],ℝ,ℝ) ∧ ∀x∈[a,b](f(x)≠c) ⟹ [(∀x∈[a,b] f(x)>c) ∨ (∀x∈[a,b] f(x)<c)]` — this is verbatim the Darboux Property's conclusion (from the same file's `thm:darboux-property`, further down), not anything about boundedness. Clear copy-paste contamination between two unrelated nodes in the same file | Not fixed |
| 49 | 🟡 (structural/completeness) | `global-theorems/notes-ivt.tex` — `thm:extreme-value-theorem`, `thm:bolzano-intermediate-value` missing "Negated quantified statement" and "Negation predicate reading" blocks; `thm:location-of-roots` missing those two plus "Predicate reading" (only has Standard + duplicated Failure-modes + Interpretation); `thm:image-of-closed-bounded-interval` missing all of Negated quantified statement, Negation predicate reading, AND Failure modes (only Standard + Predicate + Interpretation). Recurs in `uniform-continuity/notes-uniform-continuity.tex`: `thm:algebra-of-uniform-continuity-general` and `prop:uniform-continuity-cauchy` each have only Standard + Predicate + Interpretation (no Negated/Negation-predicate/Failure-modes at all); `thm:bilipschitz-inverse-is-lipschitz` likewise only Standard + Predicate + Interpretation; `prop:lipschitz-implies-uc` folds what should be separate Negated-quantified-statement/Failure-modes content (including the $\sqrt{x}$-on-$[0,1]$ counterexample showing Lipschitz $\Rightarrow$ UC is strict) directly into its single "Interpretation" remark instead of using the book's own block taxonomy. Recurs heavily in `monotone-functions/notes-monotone-functions.tex`: `thm:monotone-one-sided-limits`, `prop:monotone-discontinuities-first-kind`, `cor:jump-intervals-for-monotone-discontinuities`, `thm:monotone-discontinuities-countable`, `prop:continuous-injective-iff-strictly-monotone`, and `thm:continuous-inverse-theorem` all have only Standard + Predicate + Interpretation (no Failure-modes block at all in any of these six); `monotone-functions/notes-limsup-functions.tex` — `prop:limsup-geq-liminf-function` likewise has only Standard + Predicate + Interpretation; `gauge/notes-gauge.tex` — `lem:every-point-covered-by-tag` likewise has only Standard + Predicate + Interpretation, no Failure modes; `approximation/notes-approximation.tex` — `thm:piecewise-linear-approximation`, `thm:weierstrass-approximation`, `def:bernstein-polynomial`, `thm:bernstein-approximation` all have only Standard + Predicate + Interpretation, no Failure modes. Continues into the Differentiation chapter: `chain-rule/notes-chain-rule.tex` — `thm:chain-rule`, `thm:leibniz-rule`, `thm:faa-di-bruno` all have only Standard quantified statement + Interpretation (no Predicate reading, Negated, Negation-predicate, or Failure-modes at all) | Several theorem nodes across the Continuity chapter have visibly less block-enhancement coverage than the norm established elsewhere in the book (which is usually Standard/Predicate/Negated/Negation-predicate/Failure-modes/Interpretation, six blocks). Mathematical content of the theorem statements themselves is correct in every case checked so far; this is a completeness gap in the pedagogical scaffolding, not a math error | Not fixed |
| 50 | 🟠 | `uniform-continuity/notes-uniform-continuity.tex` — `thm:bilipschitz-inverse-is-lipschitz`, Predicate reading block | Calls `IsBiLipschitz(f⁻¹, f(A), 1/K, 1/α, c, C)` with six arguments, the last two being `c, C` — variables that are never defined anywhere in this theorem (no `c`, `C` appear in the theorem statement, which only uses `α, K`). This also doesn't match `def:bi-lipschitz`'s own Predicate reading, which calls `IsBiLipschitz(f,A,ℝ,ℝ,ℝ,ℝ)` (four generic `ℝ` placeholders, not the constants themselves). The two usages of the same predicate name have inconsistent arity/argument conventions, and this instance references undefined symbols | Not fixed |
| 51 | 🟠 (completeness) | `limits-at-infinity/notes-limits-at-infinity.tex` + its `index.tex` toolkit table | The toolkit table promises "Sequential criterion" (→ `prop:sequential-criterion-limits`) and "Limit laws" (→ `thm:limit-sum`) for limits at infinity, but both hyperlinks point to the pre-existing finite-point versions proved in the `limits` section (`notes-limits.tex`, `notes-algebra-of-limits.tex`) — no theorem in this file (or anywhere else found so far) actually states or proves that the sequential criterion or the sum/difference/product/quotient limit laws extend to $x\to+\infty$. A reader is left to assume the extension holds by analogy rather than being shown it. Separately, `def:limit-at-infinity` only defines the limit as $x\to+\infty$; despite `def:infinite-adherent-points` treating $+\infty$ and $-\infty$ symmetrically, there is no companion definition for $\lim_{x\to-\infty}f(x)$ anywhere in this file. Flagging as a missing-content gap (candidate for ADDITIONS.md) rather than an error, since nothing stated is actually wrong | Not fixed |

## Differentiation chapter (Ch.10* — Book II)

| # | Severity | Location | Issue | Fixed? |
|---|----------|----------|-------|--------|
| 52 | 🟠 | `secant-tangent/notes-secant-tangent.tex` — `def:secant-line`, Predicate reading block; recurs verbatim in `derivative-geometry/notes-shape-questions.tex` — `def:convex-function`, Predicate reading block (see also #58 below, same section); recurs three more times in `taylor-expansion/notes-taylor-expansion.tex` — `thm:taylor-theorem-lagrange-remainder`, `prop:flat-function`, and `taylor-expansion/notes-differential.tex` — `thm:differential-and-derivative-agree` (each paired with the same generic orphaned `f:A→ℝ, Derivative(D,f,a,ℝ,ℝ)` fragment described in #57) | The block's prose is unfilled template boilerplate — "The displayed quantified statement is the predicate-level form of label: its hypotheses name the input conditions, and its conclusion names the asserted structure or relation" — literally the word "label" left un-substituted, rather than actual exposition. In `def:secant-line` the formula underneath, `SecantLine(ℓ,f,a,b)`, also introduces variables `ℓ,a,b` that don't match the definition's own `x_1,x_2` notation. Confirmed as a real recurring template-placeholder bug, not a one-off, now that it's shown up twice verbatim in unrelated chapters | Not fixed |
| 53 | — (completeness) | `secant-tangent/notes-secant-tangent.tex` — `def:secant-line`, `def:difference-quotient`, `def:tangent-line` | None of the three nodes in this section have Negated quantified statement, Negation predicate reading, or Failure modes blocks (`def:difference-quotient` additionally lacks a Predicate reading). This is the sparsest block-enhancement coverage found in the audit — likely acceptable given the section's own stated role as informal motivation before the formal derivative definition, but noted for consistency with ISSUES.md #49's tracking of this pattern elsewhere in the book | Not fixed |
| 54 | 🔴 | `derivative-definition/notes-derivative-definition.tex` — the "formal obstruction" half of the Failure-modes block, in `def:topological-definition-of-derivative-at-a-point`, `def:sequential-definition-of-derivative-at-a-point` (×2), `thm:derivative-equivalence`, `prop:derivative-h-form-equivalence`, `thm:differentiable-implies-continuous` (×2), and `thm:uniqueness-of-the-derivative`; recurs in `one-sided-derivatives/notes-one-sided-derivatives.tex` — `def:left-hand-derivative`, `def:right-hand-derivative`, `thm:differentiability-and-one-sided-derivatives`; recurs again in `algebra-of-derivatives/notes-inverse-function.tex` — `thm:inverse-function-theorem-one-variable`, and `algebra-of-derivatives/notes-lhopital.tex` — `thm:lhopital-zero-over-zero`, `thm:lhopital-infinity-over-infinity` — now 14 instances across 12 distinct nodes | Every one of these instances displays the exact same formula, verbatim: `∃ε₀>0∀δ>0∃x∈A(0<|x-a|<δ∧|Q_f(x)-D|≥ε₀)` / `¬Derivative(D,f,a,ℝ,ℝ)`, labeled `[Derivative.]`. In the two newest occurrences it is at its most disconnected yet: the Inverse Function Theorem's own failure mode is "$f'(c)=0$" or "$f'$ discontinuous at $c$" (both correctly described in that node's own Exposition prose, right next to the mismatched formal block), and L'Hôpital's actual failure modes are "form not indeterminate," "$\lim f'/g'$ doesn't exist," or "$g'$ vanishes" (also correctly described in prose) — neither theorem is even about a single function's differentiability failing at a point, yet both get the single-point ε-δ-differentiability obstruction pasted in as their "formal obstruction." This is the correct failure condition only for `def:derivative-at-a-point` itself (where it appears once, appropriately, using `L`/`c` notation — though even there the formula switches to unexplained `a`/`D`/`Q_f` variables never defined in this file). Everywhere else it is simply wrong for the node it's attached to: `def:topological-definition-of-derivative-at-a-point`'s formal obstruction should negate `Derivative_top` via neighbourhoods, not this ε-δ formula; `def:sequential-definition`'s should negate `Derivative_seq` via a witness sequence (which the node's own Exposition text correctly describes, but the formal block ignores); `thm:uniqueness-of-the-derivative`'s should be about `L ≠ L'`, not a single derivative's failure condition. This is a stronger defect than the Continuity chapter's ISSUES.md #45 pattern (mere duplication of matching content) — here the reused block is copy-pasted into nodes it does not describe, so every "formal obstruction" formula past the first instance is factually mismatched with its own theorem | Not fixed |
| 55 | — (cosmetic) | `derivative-definition/notes-derivative-definition.tex` — `thm:uniqueness-of-the-derivative`; recurs in `chain-rule/notes-caratheodory.tex` — `thm:caratheodory-characterization-of-differentiability` | The theorem box has two consecutive, identical `\hyperref[prf:...]{...}` "go to proof" links back to back — a duplicated-link typo, not a content issue | Not fixed |
| 56 | 🟡 | `linear-approximation/notes-linear-approximation.tex` — `def:first-order-approximation-at-a-point` vs. `secant-tangent/notes-secant-tangent.tex` — `def:tangent-line` | Both nodes use the predicate name `HasLinearApproximation`, with incompatible arity/usage: `def:tangent-line` writes `HasLinearApproximation(f,c) = f(c)+f'(c)(x-c)` (2 args, equated to a formula, as if it names the approximating function itself), while `def:first-order-approximation-at-a-point` writes `HasLinearApproximation(f,a,L)` (3 args, used as a bare Prop with a separate "with approximating map" annotation). Same predicate name, two different conventions for what it takes and what it means, in two files that both cite the same underlying idea | Not fixed |
| 57 | — (orphaned content) | `linear-approximation/notes-linear-approximation.tex` — `def:differentiability-linear-approximation`, Predicate reading block; recurs in `graph-reading/notes-understanding-derivatives.tex` — `thm:zero-derivative-implies-constant`, `cor:equal-derivatives-constant-difference`, `thm:first-derivative-test-maximum`, `thm:first-derivative-test-minimum`, all Predicate reading blocks | Declares `f':A→ℝ` (or similar) but never uses it; the block then tacks on an unrelated fragment `f:A→ℝ, Derivative(D,f,a,ℝ,ℝ)` (sometimes preceded by legitimate prose that already answers the node's own question), using an undefined `D` that doesn't connect to anything else in the node. Reads like a leftover template fragment rather than content specific to each node — same flavor of defect as ISSUES.md #54 (mismatched boilerplate), and now confirmed as a recurring pattern across 5 nodes in 2 different chapters | Not fixed |
| 60 | — (structural) | `graph-reading/notes-understanding-derivatives.tex` — `thm:nonincreasing-iff-nonpos-derivative`, Interpretation block | The formula `∃x∈I(f'(x)>0) ⟹ ¬OrderReversing(f,I)` is displayed twice in a row, verbatim — the same duplicated-content pattern as ISSUES.md #45, but occurring inside an "Interpretation" block rather than "Failure modes" | Not fixed |
| 58 | 🟡 (recurring pattern) | `derivative-definition/notes-derivative-definition.tex` — `Derivative(L,f,Point(c,A),ℝ,ℝ)` (5-arg, `L` first, `c` wrapped in `Point`); `mean-value-theorem/notes-mean-value-theorem.tex` — `thm:rolles-theorem` uses `Derivative(0,f,c,ℝ,ℝ)` (5-arg, bare `c`, no `Point` wrapper); `thm:mean-value-theorem` uses `Derivative(f,c,(f(b)-f(a))/(b-a))` (3-arg, `f` first, no type params at all); `cor:derivative-bound-implies-lipschitz` uses `IsLipschitz(f,I,ℝ,ℝ,M)`, putting the actual constant `M` in the slot `uniform-continuity/notes-uniform-continuity.tex`'s own `def:lipschitz-condition` fills with a generic `ℝ` (`IsLipschitz(f,A,ℝ,ℝ,ℝ)`) | The `Derivative` predicate alone is now called with three different arities/argument-orders across the book (5-arg `Point`-wrapped, 5-arg bare, 3-arg), and `IsLipschitz` with two different argument conventions for its constant slot. Same family of defect as #50 (bi-Lipschitz arity mismatch) and #56 (`HasLinearApproximation` arity mismatch) — the "Predicate reading" formalism drifts in calling convention from file to file rather than being pinned to one fixed signature per predicate name | Not fixed |
| 59 | 🟡 | `derivative-geometry/notes-derivative-geometry.tex` — `thm:necessary-condition-extremum` and `cor:relative-extremum-necessary-condition` | These are literally the same proposition stated twice in a row: the theorem concludes "`¬IsDifferentiable(f,c,ℝ,ℝ) ∨ Derivative(0,f,c,ℝ,ℝ)`" and the immediately-following "corollary" concludes "`Derivative(0,f,c,ℝ,ℝ) ∨ ¬IsDifferentiable(f,c,ℝ,ℝ)`" — the same disjunction with the two disjuncts swapped (∨ is commutative, so these are the identical proposition). A corollary is expected to derive something new or narrower from its parent theorem; here it adds nothing, it just restates the theorem with its clauses reordered | Not fixed |
| 61 | — (completeness) | `graph-reading/notes-second-order-shape.tex` — all six nodes (`def:second-derivative`, `def:higher-derivatives`, `thm:second-derivative-convexity-test`, `thm:second-derivative-concavity-test`, `thm:second-derivative-test`, `prop:inflection-point-necessary-condition`) | Mathematical content is correct throughout (spot-checked, including the correct caveat that `|x|` is convex but not twice differentiable at 0, and the correct note that the inflection-point converse can fail) — flagging only the same #49-style completeness gap: every node in this file has just Standard-quantified-statement + Interpretation, none of Predicate reading / Negated quantified statement / Negation predicate reading / Failure modes | Not fixed |
| 62 | 🟠 | `graph-reading/notes-darboux.tex` — `thm:darboux`, Predicate reading block | Same unfilled template-placeholder bug as #52 ("the predicate-level form of label" — literal, unsubstituted), and the formula underneath is the same orphaned fragment pattern as #57: declares `f:A→ℝ` (never used) and `Derivative(D,f,a,ℝ,ℝ)` (undefined `D`, and this is the single-point derivative predicate, not anything about the theorem's actual claim — that $k$ strictly between $f'(a)$ and $f'(b)$ is attained). Also has no Negated quantified statement, Negation predicate reading, or Failure modes blocks at all (only Standard + Predicate + Interpretation), continuing #49's pattern. Math content of the theorem itself, and the Standard-quantified-statement/Interpretation prose, are both correct | Not fixed |
| 64 | 🟡 (recurring pattern) | `algebra-of-derivatives/notes-algebra-of-derivatives.tex` — `cor:extended-product-rule`, `thm:finite-linear-combination-rule`, and all six "on an Interval" variants (`thm:constant-multiple-rule-interval`, `thm:sum-rule-interval`, `thm:product-rule-interval`, `thm:quotient-rule-interval`, `cor:finite-sum-rule-interval`, `cor:extended-product-rule-interval`, `cor:power-rule-special-case-interval`, `thm:finite-linear-combination-rule-interval`) — 10 nodes | Every one of these nodes' "Predicate reading" block is the exact same generic orphaned fragment, `f:A→ℝ, Derivative(D,f,a,ℝ,ℝ)`, regardless of whether the node is about an extended product, a finite linear combination, or an interval-wide sum/product/quotient — none of these formulas mention the multiple functions, the finite index `n`, or the interval `I` that the theorem actually concerns. Same #57 pattern, now the densest concentration found (10 recurrences in one file). Separately, this file also shows a live instance of the #58 arity-drift pattern within itself: `thm:constant-multiple-rule`/`thm:sum-rule`/`thm:product-rule` all write the 5-argument form `Derivative(value, function, point, ℝ, ℝ)`, but `thm:quotient-rule` switches without explanation to the 3-argument form `Derivative(function, point, value)`, and every subsequent node (`cor:finite-sum-rule`, `cor:extended-product-rule`, `cor:power-rule-special-case`, `thm:finite-linear-combination-rule`, all 8 interval variants) follows the quotient-rule's 3-arg convention instead of the constant-multiple/sum/product convention used two theorems earlier in the same file. Math content of every rule statement itself (constant-multiple, sum, product, quotient, finite sum, extended product, integer power, finite linear combination — pointwise and interval forms) is correct, including all stated hypotheses ($c$ a limit point, $g(c)\neq0$, denominator nonvanishing on $I$) | Not fixed |
| 63 | 🟠 | `graph-reading/notes-smoothness-classes.tex` — `def:class-ck`, `def:class-cinfty`, `def:class-comega`, `thm:smoothness-tower`, `def:class-c11`, `thm:c11-placement` (6 of the file's 7 nodes) | This file (Class $C^1$ through $C^\omega$, and $C^{1,1}$) is otherwise the mathematically cleanest, most valuable content found so far — every theorem/definition statement checked (smoothness-tower's strict-inclusion witnesses $x^k\lvert x\rvert$ and $e^{-1/x^2}$; the $C^{1,1}$-vs-$C^2$ separators $x\lvert x\rvert$ and $\lvert x\rvert^{4/3}$; the compact-interval placement $C^2(K)\subseteq C^{1,1}(K)\subsetneq C^1(K)$) is correct — but it is the single densest concentration of the block-enhancement defects tracked elsewhere: (a) every one of these 6 nodes' "Failure modes" block displays its "predicate-level failure condition" formula twice in a row, verbatim — the #45/#60 duplication pattern; (b) every "Negation predicate reading" block declares an unused `f':A→ℝ` before the actual content — the #57 orphaned-declaration pattern; (c) `def:class-comega`'s "Predicate reading" block contains the literal unfilled "the predicate-level form of label" placeholder text (the #52 bug) **and** the formula pasted underneath it, `IsClassC(f,k,I)`, is copy-pasted from the generic $C^k$ node and says nothing about analyticity/power-series convergence — the theorem this block is attached to has no free variable `k` at all; (d) most seriously, `def:class-c11`'s "Negation predicate reading" block states `¬IsClassC(f,k,I)` — the *generic* $C^k$-failure formula with a dangling unbound `k` — copy-pasted in verbatim rather than the node's own correct negation (already given two blocks above, in its own Negated-quantified-statement block: `f∉C^1(I) ∨ ∀L≥0∃x,y(...)`unLipschitz). Flagging as one consolidated entry rather than 6 separate ones since it's the same three bugs recombined across every node | Not fixed |

## Integration chapter (Ch.11* — Book IV, the only chapter never previously audited)

| # | Severity | Location | Issue | Fixed? |
|---|----------|----------|-------|--------|
| 65 | 🟡 (structural, chapter-wide) | All 9 sections (`partitions`, `cauchy-integral`, `riemann-integral`, `darboux-integral`, `measure-zero`, `riemann-stieltjes`, `henstock-kurzweil`, `mcshane-integral`) | Unlike every other audited chapter (which run at 4-6 of 6 block-enhancement types per node), this entire chapter runs at 2/6 almost everywhere: only `Standard quantified statement` + `Interpretation` on nearly every node, with `Predicate reading` present on only a handful (`def:cauchy-integral`, `thm:darboux-criterion`, `def:measure-zero`, `def:delta-fine-tagged-partition`, `def:mcshane-fine-tagged-partition`, a few HK/McShane theorems), and **zero** nodes anywhere in the chapter carrying `Negated quantified statement` or `Negation predicate reading`. Reads as though this chapter's authoring pass never received the enhancement-block pass every other chapter got. Worst of all: **no node in the entire chapter has a block literally titled `[Failure modes]`** — see #66 | Not fixed |
| 66 | 🟠 (structural — undermines the chapter's own stated purpose) | Every integral-definition node: `def:cauchy-integral`, `def:riemann-integral`, `def:darboux-integrability`, `thm:lebesgue-criterion-riemann-integrability`, `def:riemann-stieltjes-integral`, `def:henstock-kurzweil-integral`, `def:mcshane-integral` | This chapter's entire narrative arc is integral-fails-here → next-integral-fixes-it (explicitly per its own "Worked Failure Mode" / "What the Failure Motivates Next" subsections), and the actual failure-mode mathematics IS present and — this is the good news — genuinely correct and specific in every instance checked: the step function for Cauchy→Riemann, Thomae's function and Dirichlet's function for Riemann/Darboux, the oscillation-set argument for the Lebesgue criterion, the shared-jump tag-dependence counterexample for Riemann–Stieltjes, and $F(x)=x^2\sin(1/x^2)$ (conditionally HK-integrable, not Lebesgue-integrable) for HK vs. McShane/Lebesgue. But none of it lives in a `[Failure modes]` remark attached to the node it motivates — it all sits in unlabeled `[Interpretation]` remarks under detached `\subsection*{Worked Failure Mode}` headings with no `\label`, disconnected from the specific integral-definition node a reader (or a citation, or this Lean port) would look under. Recommend re-tagging these as proper `[Failure modes]` blocks bound to their definitions | Not fixed |
| 67 | 🟠 | `cauchy-integral/notes-cauchy-integral.tex` — `def:cauchy-integral` vs. `thm:cauchy-integral-linearity`, `thm:cauchy-integral-monotonicity`, `thm:cauchy-integral-triangle-inequality`, `thm:continuous-cauchy-integrable` | `def:cauchy-integral` formalizes "f is Cauchy-integrable" as `HasDefiniteIntegral(f,[a,b],CauchyCriterion,L)` (4-arg, explicit value `L`), but every downstream theorem instead calls `IsIntegrable(f,[a,b],CauchyCriterion)` (3-arg, no `L`) for the same notion — two different predicate names/arities for "f is Cauchy-integrable," never reconciled within the file. The Lean port (`CauchyIntegral.lean`) picks one canonical predicate (existential-over-`L`, matching `IsIntegrable`'s style) rather than reproducing the split | Not fixed |
| 68 | 🟡 | `riemann-integral/notes-riemann-integral.tex` — entire file | Unlike the Cauchy section immediately before it (which introduces `HasDefiniteIntegral`/`IsIntegrable`), this section never introduces any named predicate for "f is Riemann-integrable" — every Standard-quantified-statement is prose/quantifiers only, with no `\operatorname{...}` at all. A formalization-style inconsistency between two adjacent sections of the same chapter | Not fixed |
| 69 | 🔴 | `henstock-kurzweil/notes-henstock-kurzweil.tex` — `def:delta-fine-tagged-partition`, `def:henstock-kurzweil-integral` vs. `mcshane-integral/notes-mcshane-integral.tex` — `def:mcshane-fine-tagged-partition`, `def:mcshane-integral` | The predicate `IsDeltaFine((P,ξ),δ,[a,b])` is defined with **tag-in-slab** semantics in the HK file (`ξ_i ∈ [x_{i-1},x_i]` required, inherited from the ordinary tagged-partition definition) and reused **verbatim, same name, same arity**, in the McShane file for a genuinely different relation that explicitly **drops** the tag-in-slab requirement — the entire point of McShane's construction is that tags may sit outside their own subinterval. Two logically incompatible relations sharing one name is a hard correctness bug, not just a style issue: any theorem citing `IsDeltaFine` after both sections are in scope is ambiguous. The single 🔴 of this chapter. Lean port uses distinct names, `IsHKDeltaFine` and `IsMcShaneDeltaFine` | Not fixed |
| 70 | 🟡 | `mcshane-integral/notes-mcshane-integral.tex` — `def:mcshane-fine-tagged-partition`, Predicate reading block | `TaggedPartition((P,ξ),P,[a,b])` passes `P` twice — once inside the tuple `(P,ξ)`, once again as a bare second argument — reads as a copy/paste arity error | Not fixed |
| 71 | — (text corruption, 3 instances) | `cauchy-integral/notes-cauchy-integral.tex` — `thm:continuous-cauchy-integrable` Interpretation block (`completene` / newline / `ss`); `riemann-stieltjes/notes-riemann-stieltjes.tex` — `thm:rs-bilinearity` Standard-quantified-statement block (`an` / newline / `d`) and `thm:rs-step-integrator-finite-sum` Interpretation block (`cum` / newline / `umulative`) | Single words split mid-token by a stray newline — renders fine in compiled LaTeX (whitespace collapses) but is clear evidence of an automated rewrap/injection artifact in the source pipeline, consistent across three unrelated files in this chapter | Not fixed |
| 72 | — (structural, duplication) | `riemann-integral/notes-riemann-integral.tex` — trailing "Worked Failure Mode and Next Repair" subsection (Riemann→Darboux motivation); `henstock-kurzweil/notes-henstock-kurzweil.tex` — trailing "Worked Failure Mode" subsection (the $x^2\sin(1/x^2)$ witness for HK vs. Lebesgue) | Same #45/#54-family duplicated-block pattern, here hitting unlabeled `[Interpretation]` remarks rather than `[Failure modes]`: in both files, a terser version of the same worked example/argument is immediately followed by a fuller, better-annotated version of the identical content, back to back, with no free text between them. Content is correct in both cases; recommend merging into one block each | Not fixed |
| 73 | 🟡 (minor, unverified) | `riemann-stieltjes/notes-riemann-stieltjes.tex` — `def:riemann-stieltjes-integral` dependency list | Lists `def:bounded-variation-integration` as a dependency of the RS-*sum* definition itself, though bounded variation is only needed for the later *existence* theorem (`thm:rs-continuous-bv-existence`), not to state the RS-sum limit definition. Possibly an intentional forward-reference, possibly misplaced — flagged for your judgment, not asserted as wrong | Not fixed |

## Recurring pattern (tracked once, appears repeatedly)

| # | Severity | Occurrences | Issue |
|---|----------|-------------|-------|
| 19 | 🟡 | `thm:absolute-bound-upper-lower-bounds`, `thm:upper-lower-bounds-absolute-bound` (Bounds); `def:reciprocal-sequence`, `def:pointwise-quotient-sequence`, `def:square-root-sequence` (Sequences `notes-algebra.tex`); `thm:convergence-by-domination` (Sequences `notes-tails.tex`) — 6 instances so far | "Predicate reading" remark is a verbatim copy of the node's own "Standard quantified statement" (including prose header), instead of translating into the corpus's named-predicate notation the way every other node does |

## Legend recap

- **Severity**: 🔴 genuine math error (1 so far: #11) · 🟠 serious mismatch (4 so far: #8, #13, #15, #17) · 🟡 minor/stylistic (the rest)
- This file is updated as the audit proceeds through the remaining Sequences sections (`subsequences`, `divergence`, `liminf-limsup`, `cluster-values`, `cauchy`, `order-limits`, `applications`) and beyond.
