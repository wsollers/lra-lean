import LRA.Logic
import LRA.Identity
import LRA.Set
import LRA.SetSystems
import LRA.Relation
import LRA.Function
import LRA.Carrier
import LRA.Cardinality
import LRA.Morphism
import LRA.Operation
import LRA.Order
import LRA.UniversalAlgebra
import LRA.AlgebraicStructures
import LRA.Spaces
import LRA.Topology
import LRA.Algebra
import LRA.Arithmetic
import LRA.Analysis

import LRA.VolumeI
import LRA.VolumeII
import LRA.VolumeIII
import LRA.VolumeVII

/-!
Repository-level aggregate import.

The subject routers come first and are the load-bearing entry point: subjects own
the declarations (§1.1), so build coverage must follow the subject tree rather
than depend on which subjects a volume happens to cite. The volume aggregators
follow as the curricular view (§2.6).
-/
