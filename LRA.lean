import LRA.All

/-!
Backward-compatible repository aggregate.

Prefer:

- `LRA.Core` for core mathematics,
- `LRA.ExamplesFailures` for quarantined examples and failures,
- `LRA.All` for the maximal aggregate.
-/
